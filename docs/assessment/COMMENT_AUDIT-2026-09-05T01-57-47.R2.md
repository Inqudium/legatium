# Comment Audit (Runde 2): Legatium (legatium-common, legatium-restclient-logging, legatium-webclient-logging)

1. Codebase identification
   - **Repository:** `https://github.com/Inqudium/legatium.git`
   - **Commit hash:** `683ec01cba2e604d503e5c37ea0b56f343a62d0f` (full)
   - **Reference (branch/tag):** `refs/heads/main`; kein Tag am Commit (`git describe --tags` → `1.0.0-27-g683ec01`, 27 Commits nach `1.0.0`)
   - **Working tree:** sauber zum Analysezeitpunkt (2026-09-16, 23:10 Europe/Berlin); `main` synchron mit `origin/main` (Merge von PR #8 um 22:54)
   - **Vorgängerbericht (Kette):** `./docs/assessment/COMMENT_AUDIT-2026-09-05T01-57-47.md` zu Commit `a8a7b917bb3283964172fa895092052e56a5aa60`; 38 Commits liegen dazwischen, darunter drei Feature-Commits (ADR-0009, ADR-0010, ADR-0011) und zwei Fix-Runden vom heutigen Tag (Defekt-Analyse `4fbc7b1`, Code-Style-Analyse `c30c14c`). Dieser Bericht ist Runde 2 derselben Kette: er führt die 16 Befunde des Vorgängers in einem Register mit verifiziertem Status, vergibt neue IDs ab 17 und ergänzt den Vorgänger, ohne ihn zu ersetzen
2. Scope of the analysis
   - **Included:** `./legatium-common/src/main/kotlin/`, `./legatium-restclient-logging/src/main/kotlin/`, `./legatium-webclient-logging/src/main/kotlin/` (37 Kotlin-Dateien, 4.701 Zeilen, 1.908 Kommentarzeilen = 41 %; vollständig gelesen)
   - **Excluded:** Build-Ausgaben (`./target/`, `./*/target/`, `./docs/target/`), die Markdown-Dokumentation als Gegenstand (`./docs/`, `./*/docs/`, `./*.md` - nur als Referenzziel konsultiert; Abweichungen dort werden als "außerhalb des Scopes" notiert, nicht als Befund geführt), Workflows und Skripte unter `./.github/`, Testressourcen, das eigenständige `./consumer-smoke/` (nur inventarisiert), frühere Dateien unter `./docs/assessment/` (schreibgeschützte Historie)
   - **Test code in scope:** ja - `./legatium-common/src/test/{kotlin,java}/`, `./legatium-restclient-logging/src/test/{kotlin,java}/`, `./legatium-webclient-logging/src/test/kotlin/` (61 Dateien, 3.480 Kommentarzeilen); mechanisch vollständig inventarisiert (Rationale-Blöcke, Stufenlabels, Klassen-KDoc, Twin-Duplikate, Textreferenzen), in der Tiefe gelesen überall dort, wo eine Produktions-KDoc "pinned by …" behauptet (jeder genannte Pin gegen den Testkörper verifiziert), plus Stichproben (`TestSupport.kt` beider Twins, `IntegrationFixture.kt`, beide `TwinContractTest`, beide `UriTemplateAttributeTest`, `CountingCorrelationIdGeneratorTest`, `HeaderValueMaskerTest`, `ClientLogFieldTest`, `MdcScopeTest`, `FailOpenDiagnosticsTest`, `BodyLogModeTest`, `SharedContractTest`, `AmbientContextRestorerTest`, `CallerMdcSnapshotTest`, `ClientLoggingAutoConfigurationTest` beider Twins, die drei Fuzz-Ziele, `Tarpit.kt`, Auszüge der großen Filter-/Interceptor-Suiten). Tests zählen als eigener Kommentargegenstand UND als Beispielträger
   - **Public API (sharpen instead of shorten):** die nicht-`internal` Deklarationen - `ClientLoggingProperties`, `HeaderLogProperties`, `BodyLogMode`, `BodyReadState`, `HeaderValueMasker`, `CorrelationIdGenerator`, `NanoTimeSource`, `MdcKeys` in `./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/`; `ClientRequestLoggingInterceptor` und `ClientLoggingAutoConfiguration` in `./legatium-restclient-logging/src/main/kotlin/eu/inqudium/legatium/restclient/logging/`; `ClientRequestLoggingFilter` und `ClientLoggingAutoConfiguration` in `./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/`. Nur diese rendert Dokka; ihre KDoc muss ohne Sprung in `internal`-Typen tragen
   - **Internal code (reduction policy):** alle `internal`-Deklarationen (43 von 56 Top-Level-Deklarationen), einschließlich der in beide Twin-Jars geshadeten Common-Klassen
3. Analysis environment & tools
   - **Target environment:** Java 21 (Artefakt-Ziel), Kotlin 2.4.20, Spring Boot 4.1.1 (spring-web 7.0.9); Analyse-JDK Oracle 26.0.1
   - **Build system:** Apache Maven 3.9.15, Drei-Modul-Reaktor plus eigenständiges `./consumer-smoke/`
   - **Documentation tool:** Dokka 2.2.0 (`dokka-maven-plugin`), **`failOnWarning = true`** (Root-POM `pluginManagement`, Zeile 268), `documentedVisibilities` Standard (public). Symbolische `[…]`-Referenzen in KDoc gelten als abgesichert, auch in `internal`-Deklarationen (Nachweis aus Runde 1). **Neu seit Runde 1:** Dokka läuft jetzt auch im CI-Job `build` (`./.github/workflows/ci.yml:50`, `dokka:dokka` für beide Twins) - eine kaputte Referenz ist damit ein roter PR-Check, nicht mehr erst ein roter Docs-Lauf nach dem Merge; die Guardrail-Empfehlung aus Runde 1 ist umgesetzt. Weiterhin ungeprüft: `[…]`-Links in `//`-Zeilenkommentaren (Dokka liest sie nicht - Bestand jetzt 11 bis 15 je nach Zählmuster, Runde 1: 0), Backtick-Namen, Prosa-Verweise, Dateinamen, Aussagen über den anderen Twin und über das Schwesterprojekt limesium
   - **Convention sources:** kein `./CLAUDE.md` im Repository; `./CONTRIBUTING.md` ("Code style": "comments should state constraints the code can't express, not narrate the code"; "Tests": Drei-Fragen-Rationale-Block plus `Given/When/Then`, Einzeiler-Form zulässig; "KDoc `[Symbol]` links are checked"); `./docs/adr/ADR-0001` bis `ADR-0011` (drei neue seit Runde 1: 0009 Adapter-Name, 0010 Ambient-Context-Restauration des reaktiven Twins, 0011 MDC-Snapshot des blockierenden Twins); kein dokumentiertes Präfix-Vokabular (Pass 2 (i) aus Runde 1 nicht umgesetzt)
   - **Analysis tools used:** vollständige Quelltextlektüre der 37 Produktionsdateien in drei modulweisen Lesedurchgängen mit Verifikation jedes Test-Pins; `grep`, `git log --name-only` (Churn über die gesamte Historie: 75 Commits, 2026-09-03 bis 2026-09-16), `git blame`/`git log -S` an jeder driftverdächtigen Zeile; Python-Inventar (Kommentarzeilen, größte Blöcke, Twin-identische Zeilen, Emphasis-Wörter, Links, Textreferenzen, Datumsanker, undokumentierte Deklarationen, überlange KDoc-Zeilen); Cross-Repo-Prüfung lesend gegen `~/IdeaProjects/limesium` (Commit-Historie eingeschlossen); spring-web-7.0.9-Sources für `hasMessageBody()`; die Zählskripte des Test-Evidence-Generators für die Rationale-Vollständigkeit. Kein Build gestartet; kein Werkzeug hat den Arbeitsbaum verändert
4. Placement & Output
   - **Working directory (workdir):** `/home/dirk/IdeaProjects/legatium` (absoluter Referenzpunkt; alle relativen Pfade beziehen sich darauf)
   - **Report output path:** `./docs/assessment/COMMENT_AUDIT-2026-09-05T01-57-47.R2.md` (Folgebericht: Basisname des Erstberichts plus Rundensuffix, gemäß Chaining-Regel; der Erstbericht bleibt unverändert)
   - **Scope root (relative to the workdir):** `./` - die drei Modulwurzeln `./legatium-common/`, `./legatium-restclient-logging/`, `./legatium-webclient-logging/`
   - **Path convention for locations:** `<path relative to the workdir>:<line>` (Zeilennummern zum oben genannten Commit)
   - **Prior analysis documents:** `COMMENT_AUDIT-2026-09-05T01-57-47.md` (Vorgänger dieser Kette), `CODE_STYLE-2026-09-16T21-47-00.md` und `DEFECT_ANALYSIS-2026-09-16T20-19-34.md` (heute; ihre Fixes sind Teil des analysierten Stands), die fünf Dokumente vom 2026-09-04/05 - gelesen, nicht verändert; ihre Aussagen nur nach Prüfung am aktuellen Code übernommen

---

## 1. Executive summary

Elf Tage und 38 Commits nach Runde 1 trägt die Kommentarbasis weiterhin deutlich mehr Wissen als Ballast, und die mechanischen Schuldklassen sind leer geblieben: 0 auskommentierter Code, 0 `TODO`/`FIXME`, 0 zeremonielle `@param`-Dokumentation, 0 unauflösbare KDoc-Links (jetzt auch im PR-Check gehalten), 0 Review- oder Prozentzitate im Code (Runde 1 hatte sechs), 339 von 339 Test-Rationale-Blöcken vollständig, 61 von 61 Testklassen mit Klassen-KDoc. Von den 16 Befunden der ersten Runde sind 12 aufgelöst, drei teilweise (die Twin-Duplikate, die Textreferenzen, die Cross-Repo-Aussagen) und einer ist ein Konventionsentscheid. Was die drei Feature-Commits und die zwei Fix-Runden dazwischen hinterlassen haben, ist die Schuld dieser Runde, und sie hat eine andere Gestalt als in Runde 1: nicht Beweisführung, sondern **Aussagen über einen Nachbarn, den kein Werkzeug mitliest**. Zwei davon sind falsch geworden, beide an prominenter Stelle: die KDoc des reaktiven Twins erklärt, der blockierende Twin habe "kein Gegenstück" zur Kontextrestauration - seit ADR-0011 hat er eines, am selben Tag eingeführt, und die öffentliche Filter-KDoc trägt dieselbe Vereinfachung (Befund 17); und `CorrelationHeader` verspricht, das Schwesterprojekt limesium "spiegle die Regel", während limesium seine Obergrenze am 5. September auf 128 gesenkt hat und legatium bei 200 steht (Befund 18). Beide sind seit Tagen falsch, und nichts ist rot geworden.

Das zweite Muster dieser Runde stammt aus den Fix-Runden selbst: **eine Regel wird an jedem Hop erklärt, den ein Fix berührt hat**. Die Content-Length-Vollständigkeitsregel steht fünfmal entlang einer Aufrufkette (vier der fünf Stellen von heute), die Kundenorder-Konsequenz zweimal pro Auto-Konfiguration (beide von heute), die Zustandsmaschine des reaktiven Twins dreimal in einer Datei (die dritte Kopie von heute), und der Fix des Runde-1-Befunds 6 hat die Property-KDoc kopiert, ohne die Klassen-KDoc zu kürzen. Dazu ein Trugschluss der Runde-1-Kondensation: 15 `[Symbol]`-Links wurden in `//`-Zeilenkommentare gesetzt, wo sie wie geprüft aussehen und es nicht sind (Befund 21). Die Zahl der Twin-identischen Kommentarzeilen ist von 40 auf 51 gestiegen - überwiegend legitim (öffentliche Parameter-KDoc, die Ein-Satz-Verweise, die Runde 1 selbst empfohlen hat), aber der Rest mit kanonischem Ort in `legatium-common` ist geblieben (Befund 28).

Die Schutzstellen aus Runde 1 stehen unverändert und haben Zuwachs: `ObservedBody`/`ObservedResponse` (Handover-Race mit Fehlerbild), beide `BoundedBodyCapture`-Modelle, die Chronik mit Fehlerbild in `CapturingClientHttpResponse`, `Tarpit.kt`; neu dazu die `IntegrationFixture`-Begründung (warum `@BeforeEach`, mit Fehlerbild) und die Reihenfolge-Invariante der Wiring-Schritte im Interceptor. Kein Hygiene-Pass darf sie anfassen.

**Konklusion.**

1. **Zustand:** wissenstragend, dicht (41 % Kommentaranteil, Runde 1: 39 %), mechanisch sauber. Schwerpunkt der Schuld: **Drift in Nachbarschaftsaussagen** (2 falsche, 1 unvollständige Liste, 1 veraltete Exactly-once-Aussage, 1 veralteter Operatorname), **Wiederholung derselben Regel entlang der Aufrufkette und über Klasse/Member bzw. Feld/Konstante** (≈ 15 Paare, ≈ 55 Zeilen, vier davon aus den heutigen Fix-Runden) und **Scheinabsicherung** (`[Symbol]` in Zeilenkommentaren). Still falsch werden können die ≈ 18 Symmetrie-Aussagen zwischen den Twins, die 7 limesium-Aussagen, die ≈ 20 Prosa- und Backtick-Verweise und die 15 ungeprüften Links - zusammen rund 3 % der Kommentarzeilen; der Rest ist Entscheidungswissen, das nur mit der Entscheidung altert, oder durch Dokka und die Test-Pins abgesichert (jeder in Produktions-KDoc genannte Pin wurde verifiziert; einer fehlt, Befund 23).
2. **Reduzierbarkeit:** durch Kondensation realistisch **80 bis 100 Produktionskommentarzeilen (4 bis 5 %)** - common ≈ 25-30, RestClient ≈ 25-32, WebClient ≈ 32-37 -, begrenzt durch die Twin-Regel (Ein-Satz-Verweise müssen in beiden Twins stehen), die Public-API-Grenze (drei Befunde FÜGEN Sätze hinzu: 19, 20, 44) und einen fehlenden Anker: die Teardown-Regel der Emitter ("nach der Zeile auf dem Logger ist ein Restaurationsfehler Buchhaltung, nie eine verlorene Zeile") hat keinen kanonischen Ort (Befund 29). **Dekompositionen: keine** - alle Inline-Blöcke ≥ 4 Zeilen in `intercept`, `filter`, `wireExchange` und `emitExchange` begründen die jeweils nächste Anweisung; das Zeilenkonto ändert sich durch diese Kategorie nicht. Der begrenzende Faktor ist derselbe wie in Runde 1, nur schärfer: es gibt kein Werkzeug, das eine Aussage über den anderen Twin oder über limesium prüft, und kein Präfix-Vokabular.
3. **Wesentliche Risiken und Anomalien:**
   - 🟠 `AmbientContextRestorer.kt:19-20` und die öffentliche `ClientRequestLoggingFilter`-KDoc `:46-47` behaupten, der blockierende Twin habe kein Gegenstück - falsch seit ADR-0011 (Befund 17).
   - 🟠 `CorrelationHeader.kt:15-16` behauptet Konsistenz mit limesium bei 200 gegen 128 Zeichen; ADR-0002 trägt denselben Satz; kein Literal-Pin in beiden Repositories (Befund 18).
   - 📚🔴 an Score-4/5-Stellen, die kein Hygiene-Pass anfassen darf: `ObservedResponse.kt:15-34`, `ObservedBody.kt:17-37`, beide `BoundedBodyCapture`-Klassen-KDocs, `CapturingClientHttpResponse.kt:14-28`, `ClientRequestLoggingInterceptor.kt:320-326` (Wiring-Reihenfolge), `FailOpenDiagnostics.kt:35-42`, `Mdc.kt:20-27,34-43`, `ClientLoggingMetrics.kt:109-116`, `RequestTarget.kt:9-18`, `Timeouts.kt:9-18`, `Tarpit.kt:8-35`, `IntegrationFixture.kt:21-25`.
   - Systemisch: ungeprüfte Symmetrie-Aussagen zwischen den Twins (P6, neu), Regelwiederholung entlang der Aufrufkette (P2, neue Gestalt), `[Symbol]` in Zeilenkommentaren als Scheinabsicherung (P3, neue Untergruppe), ADR-Inhalt am Code paraphrasiert (P8, neu).

## 2. Scope & methodology

**Analysiert:** alle 37 Produktionsdateien der drei Module vollständig; der gesamte Testbestand mechanisch und - über Runde 1 hinaus - jeder in Produktions-KDoc behauptete Test-Pin gegen den Testkörper. **Nicht analysiert:** die Markdown-Dokumentation als Gegenstand (Abweichungen dort in den Befunden als "außerhalb des Scopes" notiert), Workflow-YAML, Python-Skripte, `pom.xml`-Kommentare, `./consumer-smoke/` in der Tiefe.

**Test code:** im Scope. Die Tests sind weiterhin der einzige Beispielträger (kein `@sample`, keine Beispielblöcke); wo Produktions-KDoc "pinned by …" sagt, ist die Testklasse der gemeinte Träger - alle genannten Pins existieren und pinnen das Behauptete, mit einer Ausnahme (Befund 23: ein Zustand, den kein Test pinnt) und einer veralteten Ortsangabe (Befund 35).

**Grenze Public API / intern:** unverändert die 13 nicht-`internal` Deklarationen des Metadatenblocks. Die Grenze ist eingehalten; die Fix-Runden haben sie in einem Punkt verletzt, den Runde 1 nicht sehen konnte: die öffentlichen Entry-Point-KDocs beschreiben den JEWEILS ANDEREN Twin (Differenzliste des Filters, "MDC coverage" des Interceptors), und diese Abschnitte sind nach ADR-0010/0011 nicht nachgezogen (Befunde 17, 19, 20). Dort gilt "schärfen", nicht kürzen - zwei der drei Befunde fügen Sätze hinzu.

**Konventionsquellen:** wie Runde 1; drei neue ADRs (0009-0011) sind Referenzziele für Adapter-Name, Ambient-Context (reaktiv) und MDC-Snapshot (blockierend). ADR-0010 wurde am Tag seiner Einführung um einen Absatz amendiert, der die "kein Gegenstück"-Aussage zurücknimmt - der Code, der die Aussage trägt, nicht (Befund 17). Kein Präfix-Vokabular; das in Runde 1 vorgeschlagene ADR existiert nicht.

**Vorhandenes Präfix-Vokabular (emergent):** `ELK:` (14 Vorkommen, alle `ClientLogFields.kt`) bleibt das einzige strukturierte Präfix; das Emphasis-Vokabular in Großbuchstaben ist auf ≈ 374 Vorkommen gewachsen (Runde 1: ≈ 296; Skript, Akronyme ausgenommen; Spitzenreiter `BEFORE` 15, `NOT` 12, `ONE` 11, `FIRST` 9, `AFTER` 8). `Invariant:`, `Workaround:`, `Assumption:` kommen weiterhin nicht vor, obwohl die Fix-Runden neue Invarianten eingeführt haben (Wiring-Reihenfolge, "origin count LAST", `@BeforeEach`-Regel der Fixtures).

**Referenzprüfung:** Dokka mit `failOnWarning` prüft `[…]`-Links in KDoc, jetzt im PR-Check. **Neu:** 11 bis 15 `[…]`-Links stehen in `//`-Zeilenkommentaren (11 mit dem engen Muster `^\s*//.*\[[A-Z]\w*(\.\w+)?\]`, 15 mit Kleinbuchstaben-Membern wie `[failOpen]`), entstanden durch die Kondensation aus Runde 1 und die heutige `withEmissionScopes`-Extraktion - Dokka liest sie nicht (Befund 21). Weiterhin ungeprüft: 10 Backtick-Test-/Dateinamen, 10 Prosa-Verweise ("see the constant", "see below", "by the rule above"), 7 limesium-Aussagen, ≈ 18 Aussagen über den anderen Twin.

**Churn-Basis:** `git log --format= --name-only` über die gesamte Historie (75 Commits, 2026-09-03 bis 2026-09-16); Spitzen: RestClient-Emitter 17, WebClient-Emitter 15, Interceptor 15, Filter 14, `ClientLoggingProperties` 10, beide Auto-Konfigurationen 9. Ein `git blame`-Altersvergleich (Median-Datum Kommentar gegen Median-Datum Code je Datei) zeigt bei 13 Tagen Historie keine Datei, deren Kommentare mehr als drei Tage älter sind als ihr Code; als Driftindikator diente daher pro Zeile `git blame`/`git log -S` an jeder Aussage über einen Nachbarn, einen Test oder einen Mechanismus - dort liegen alle vier gefundenen Drifts (Kommentar vom 3./4. September über Code, der am 5. oder 16. September geändert wurde).

**Zählbasis Inventar:** wie Runde 1 (Kommentarzeile = getrimmt mit `//`, `/*` oder in einem Blockkommentar; "Twin-identisch" = wortgleiche Kommentarzeile > 40 Zeichen nach Entfernen der Kommentarzeichen in beiden Twin-Modulen; "undokumentierte Deklaration" stichprobengeprüft - das Skript lieferte 6 Testklassen "ohne KDoc", alle sechs sind Fehltreffer durch mehrzeilige `@SpringBootTest(`-Annotationen, genau der im Prompt beschriebene Fehler; tatsächlich 0).

**Bekannte blinde Flecken:** die Tiefenlektüre der Tests deckt rund 2.500 der 11.100 Testzeilen; Cross-Repo-Aussagen wurden gegen limesium geprüft, wo eine prüfbare Behauptung vorlag (7 Stellen: 5 wahr, 1 falsch, 1 nicht prüfbar - der `HIGHEST_PRECEDENCE + 10`-Wert ist verifiziert, seine Begründung nicht). Ob 200 gegen 128 Zeichen ein Defekt ist, ist Frage der Defekt-Analyse, nicht dieses Berichts.

## 3. Statistics

**Neue Befunde dieser Runde (IDs 17-45):**

| Severity | Anzahl |
|---|---:|
| 🔴 Critical | 0 |
| 🟠 High | 2 |
| 🟡 Medium | 9 |
| 🟢 Low | 18 |
| **Gesamt neu** | **29** |
| **Systemische Muster (Kette, gültig)** | **9** (P1-P5 aus Runde 1 fortgeführt, P2 in neuer Gestalt; P6-P9 neu) |

📚-Verteilung der neuen Befunde: 📚🔴 1 (Befund 40), 📚🟠 4 (19, 23, 29, 44), 📚🟡 19, 📚⚪ 5 (35, 37, 42, 43, 45).

**Register der Kette (Befunde 1-16 aus Runde 1, verifiziert am aktuellen Commit):** 12 aufgelöst (1, 2, 4, 7, 8, 9, 10, 11, 13, 14, 15 und 6 mit Nebenwirkung), 3 teilweise aufgelöst (3, 5, 12), 1 Konventionsentscheid (16). Details in Abschnitt 5.0.

**Änderungsprotokoll Schweregrade:** Runde-1-Befund 12 (Low, Cross-Repo-Aussagen) - die Aussage in `CorrelationHeader.kt` ist inzwischen FALSCH und wird als Befund 18 mit **High** geführt (Restrisiko gestiegen). Runde-1-Befund 3 (Medium, Twin-Duplikate) - Restbestand als Befund 28 mit **Low** (Restrisiko gesunken: kanonische Orte vorhanden, Zählung stabil). Runde-1-Befund 5 (Medium, Textreferenzen) - Restbestand und Regression als Befund 37 mit **Low**, die aus der Kondensation entstandene Scheinabsicherung als Befund 21 mit **Medium** (neue Gestalt, nicht gesunkenes Risiko). Runde-1-Befund 9 (Low, Run-on-Zeilen) - an der Stelle behoben, das Muster mit 14 neuen Zeilen als Befund 45 (**Low**, unverändert).

## 4. File ranking table (Phase 1)

Score = erwartete Kommentarschuld (1-5); Kommentarzeilen / Zeilen; Churn = Commits über die gesamte Historie. Reihenfolge nach Restrisiko dieser Runde.

| Datei (relativ zum workdir) | Score | Kommentar / Zeilen | Churn | Rationale |
|---|---:|---:|---:|---|
| `./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/ClientRequestLoggingFilter.kt` | 5 | 168 / 388 | 14 | Öffentliche API, 60-Zeilen-Klassen-KDoc mit Differenzliste, die den anderen Twin beschreibt und nach ADR-0011 und dem Breadcrumb-Fix nicht nachgezogen ist (17, 19); 16 Twin-identische Zeilen |
| `./legatium-restclient-logging/src/main/kotlin/eu/inqudium/legatium/restclient/logging/ClientRequestLoggingInterceptor.kt` | 5 | 190 / 493 | 15 | Öffentliche API, 58-Zeilen-KDoc; "MDC coverage" vor ADR-0011 (20); 4 ungeprüfte Links (21); Content-Length-Regel und Request-Sample-Regel wiederholt (24, 32); Schutzstelle `:320-326` |
| `./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/CorrelationHeader.kt` | 5 | 16 / 28 | 1 | Kleinste Datei mit dem zweitgrößten Risiko: nachgewiesene Cross-Repo-Drift (18); Churn 1 heißt hier, dass niemand hinsah, während der Nachbar sich änderte |
| `./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/AmbientContextRestorer.kt` | 5 | 34 / 66 | 2 | Neu seit Runde 1; falsche "kein Gegenstück"-Aussage (17); Lokalitätsbefund (41) |
| `./legatium-restclient-logging/src/main/kotlin/eu/inqudium/legatium/restclient/logging/ExchangeLogEmitter.kt` | 4 | 108 / 388 | 17 | Höchster Churn; Levels doppelt (30), 3 ungeprüfte Links (21), veraltete Testortsangabe (35), ADR-0011-Schichtung ×4 (29) |
| `./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/ExchangeLogEmitter.kt` | 4 | 86 / 371 | 15 | "the one place" veraltet (22); ADR-0010-Schichtung ×4 (29); Levels doppelt (30); 14 Twin-identische Zeilen (überwiegend die Ein-Satz-Verweise aus Runde 1) |
| `./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/ClientLoggingMetrics.kt` | 4 | 174 / 456 | 7 | Dichtestes Entscheidungswissen; Feld/Konstante-Doppelung ×4 (26); Schutzstellen `:109-116`, `:363-366` |
| `./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/Exchange.kt` | 4 | 59 / 107 | 7 | Zustandsmaschine dreimal in einer Datei (25); 10 Twin-identische Feld-KDocs mit kanonischem Ort in common (28) |
| `./legatium-restclient-logging/src/main/kotlin/eu/inqudium/legatium/restclient/logging/{CapturingClientHttpResponse,BoundedBodyCapture}.kt` | 4 | 46 / 122, 60 / 142 | 6, 6 | Content-Length-Regel ×5 entlang der Kette, vier Stellen von heute (24); Schutzstellen (Chronik mit Fehlerbild, Single-Writer-Invariante) |
| `./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/{HeaderLogProperties,BodyCapture,ClientLoggingProperties}.kt` | 4 | 42 / 96, 34 / 72, 102 / 162 | 6, 5, 10 | Öffentliche API: Klassen/Property-Doppelung als Nebenwirkung von Runde-1-Fix 6 (27), ein ungepinnter Zustand (23), Wartungsregel ohne Test (40), `WILDCARD`/Enum-Einträge ohne eigene KDoc (44) |
| `./legatium-{restclient,webclient}-logging/src/main/kotlin/.../ClientLoggingAutoConfiguration.kt` | 3 | 47 / 114, 40 / 98 | 9, 9 | Öffentliche API; Kundenorder-Regel doppelt plus "see the constant" neben geprüftem Link, beide Twins, von heute (31) |
| `./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/{ObservedBody,ObservedResponse}.kt` | 3 | 52 / 144, 37 / 114 | 2, 1 | Schutzstellen; ObservedBody +22 Zeilen heute, davon 11 wortgleich mit dem Guide (34) |
| `./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/BoundedBodyCapture.kt` | 3 | 54 / 140 | 4 | Schutzstelle (Lock-und-Freeze); veralteter Operatorname `doFinally` (36); "tee tests" ungenau (37); `maxBytes`-Vorbedingung als Prosa (42) |
| `./legatium-restclient-logging/src/main/kotlin/eu/inqudium/legatium/restclient/logging/{CallerMdcSnapshot,Exchange}.kt` | 3 | 31 / 92, 48 / 88 | 2, 8 | Klassenregel am Member wiederholt (33); 10 Twin-identische Feld-KDocs (28); Backtick statt Link (37) |
| `./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/CorrelationIdGenerator.kt` | 3 | 36 / 72 | 5 | Öffentliche API; "see that class's documentation" auf `internal`-Klasse (38); Breiten-Grenztest weiter zweimal (39); 5 überlange Zeilen (45) |
| `./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/{ClientLogFields,Mdc,FailOpenDiagnostics,HeaderValueMasker}.kt` | 2 | 117 / 170, 37 / 105, 34 / 57, 59 / 107 | 5, 3, 2, 6 | Kanonische Orte, in Runde 1 bereinigt und stabil; Zahlwort "fourteen" (43); `MdcKeys` ohne Konstanten-KDoc (44); Cross-Repo-Pin des Fingerprints verifiziert |
| `./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/{Timeouts,Traceparent,ClientIdentity,ClientActivation,BodyLogMode,RequestTarget,NanoTimeSource,Classification,AdapterName,DeclaredCharset,NoOpScope}.kt`, `./legatium-webclient-logging/.../CapturingDecorators.kt` | 1-2 | 5-23 | 1-4 | Entscheidung + Grund in Zielform; alle Pins verifiziert; keine Befunde außer je einer überlangen Zeile |
| Tests: `TestSupport.kt` beider Twins, `IntegrationFixture.kt`, `Tarpit.kt` | 2 | 20-40 | 1-3 | Neue Schutzstelle `IntegrationFixture.kt:21-25`; `Tarpit.kt` unverändert; ADR-0003-Kopie-Rationale je einmal |
| Tests: alle `*Test.kt`, `*Contract.kt`, `*FuzzTest.java` | 2 | 3.480 | 1-15 | 339/339 Rationale-Blöcke vollständig, 61/61 Klassen-KDoc, 553 nackte / 261 beschriftete Stufenlabels, 76 Twin-identische Kommentarzeilen > 60 Zeichen; zwei ordnungsabhängige Verweise (`FilterTest:407` "(next test)", `:460` "the distinction above") |

**Pilotmodul-Empfehlung: unverändert `./legatium-restclient-logging/`.** Die Begründung aus Runde 1 gilt weiter (höchster Churn: Emitter 17, Interceptor 15; mittlere Komplexität ohne Zustandsmaschine; alle Projektmuster vertreten), und sie hat sich verschärft: vier der sechs Regelwiederholungen von P2 sind heute in diesem Modul entstanden, es trägt sieben der 15 ungeprüften Links, und die Content-Length-Kette (Befund 24) ist der beste Übungsfall für "eine Regel, ein Ort, Verweise an den Hops". Der reaktive Twin trägt zwar die beiden Drift-Befunde mit dem höchsten Risiko (17, 19), aber die sind Sofortkorrekturen außerhalb der Pässe, kein Pilotgegenstand; als komplexeste Einheit bleibt er ausdrücklich nicht der Pilot. `legatium-common` ist Träger der kanonischen Orte und wird in Pass 2 berührt.

## 5. Findings

### 5.0 Register der Kette: Befunde 1-16 aus Runde 1, Status am Commit `683ec01…`

- [x] 1. **aufgelöst.** `./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/HeaderValueMasker.kt:37-39` nennt `secret-token` → `12:930bbdc51b6aed5c`; identisch in `HeaderValueMaskerTest.kt:18`, `./docs/GUIDE.md:383` und in limesiums Test-Pin. Außerhalb des Scopes: limesiums eigene `HeaderValueMasker.kt:36`-KDoc und dessen `docs/GUIDE.md:654` tragen noch das falsche `18:…`-Literal - ein Befund für das Schwesterprojekt.
- [x] 2. **aufgelöst.** `ClientActivation.kt:9-10`: Guide-Zitat und Amendment-Datum entfernt.
- [x] 3. **teilweise aufgelöst.** Die genannten Emitter-Blöcke sind Ein-Satz-Verweise (`[MdcScope]`, `[ClientLoggingMetrics.eventEmitted]`, `[BodyLogMode]`, `internalLog`-Einzeiler); `declaredCharsetOrUtf8` lebt einmal in `DeclaredCharset.kt`. Verblieben: die `Exchange`-Feld-KDocs (10 wortgleiche Zeilen) und die `newCaptures`-Regel trotz kanonischer KDoc in common → **Befund 28**. Die Twin-identische Zählung ist von 40 auf 51 gestiegen (Abschnitt 6, P1); die Kondensation zu Ein-Satz-Verweisen hat die Links in `//`-Kommentare gesetzt → **Befund 21**.
- [x] 4. **aufgelöst.** 0 Review-, Prozent- oder "byte-identical"-Rückblicke in `ClientLoggingMetrics.kt`, `ClientLogFields.kt`, `ClientLoggingProperties.kt`; einziger Datumsanker `CorrelationHeader.kt:5` (ADR-Amendment-Datum, akzeptiert). Außerhalb des Scopes: `./docs/GUIDE.md:901` hält die Beweisführung ("converged to near-identity").
- [x] 5. **teilweise aufgelöst, mit Regression.** Fixstellen halten (`[ExchangeLogEmitter.logRequestStart]`, `[failOpen]`, `[ObservedResponse]`, `[tee]`); Backtick-Testnamen tragen einen Restsatz (akzeptiert). Neu entstanden: "see the constant" (beide Auto-Konfigurationen, heute), "by the rule above" (Interceptor `:414`, heute), "(see below)" (beide Entry Points, Fix von Befund 8), "(see ClientLogField.OUTCOME)" (RestClient-Emitter `:218`, nie erfasst), ein Backtick-Produktionssymbol `CorrelationIdGenerator.kt:8` → **Befunde 37, 38**.
- [x] 6. **aufgelöst, mit Nebenwirkung.** Property-KDoc in `HeaderLogProperties.kt:32-39` vorhanden - aber die vier Klassen-KDoc-Bullets `:13-26` blieben ungekürzt (Doppelung, **Befund 27**), und die neuen Zeilen sind 154-172 Zeichen lang (**Befund 45**).
- [x] 7. **aufgelöst.** Die Ein-Instanz-Regel steht einmal (`ClientLoggingMetrics.kt:92-98`), die drei anderen Stellen je ein Satz. Dasselbe Muster wiederholt sich eine Ebene tiefer (Feld ↔ `*_METER`-Konstante) → **Befund 26**.
- [x] 8. **aufgelöst.** Methoden-KDoc auf den drei Seams, Konstruktorparameter-KDoc beider Entry Points, Bean-Methoden-KDoc beider Auto-Konfigurationen. Nebenwirkung: "(see below)" am `meterRegistry`-Parameter → **Befund 37**.
- [x] 9. **an der Stelle aufgelöst, Muster regrediert.** `HeaderValueMasker.kt:13` und `ClientLoggingAutoConfiguration.kt:28-32` (WebClient) umgebrochen; 14 andere Einzeiler-KDocs sind 133-172 Zeichen lang → **Befund 45**.
- [x] 10. **aufgelöst.** "SIX meter families under SEVEN meter names" einmal definiert (`ClientLoggingMetrics.kt:83-84`), `SharedContractTest` und Guide verwenden es. Ein Test-Kommentar sagt noch "The six meters" (`./legatium-webclient-logging/src/test/kotlin/.../ClientRequestLoggingMetricsTest.kt:39`; Konsistenzbeobachtung, kein Befund).
- [x] 11. **aufgelöst.** `checkNotNull(eventCounters[outcome]) { … }` in `ClientLoggingMetrics.kt:210-213`, KDoc nennt die Prüfung.
- [x] 12. **teilweise aufgelöst, eine Aussage regrediert.** `CapturingClientHttpResponse.kt:18-19` trägt nur noch die lokale Entscheidung; Fingerprint-Pin (`HeaderValueMasker.kt:38-39`) in beiden Repositories verifiziert; `ClientLogFields.kt:25-27`, `Mdc.kt:9-10` wahr. `CorrelationHeader.kt:15-16` ("limesium mirrors the rule") ist seit dem 2026-09-05 falsch → **Befund 18** (Severity von Low auf High).
- [x] 13. **aufgelöst.** `CorrelationIdGenerator.kt:48` ein Satz; eine zweite Nennung des Breiten-Grenztests verbleibt `:69` → **Befund 39** (Low).
- [x] 14. **aufgelöst.** Schwellenwert-Semantik kanonisch in `ClientLoggingProperties.kt:69-72` mit `require`; beide Emitter verweisen mit `[ClientLoggingProperties.slowRequestThreshold]` - allerdings in `//`-Kommentaren → **Befund 21**.
- [x] 15. **aufgelöst.** `CountingCorrelationIdGeneratorTest.kt:11-16` trägt Klassen-KDoc; 61 von 61 Testklassen dokumentiert.
- [x] 16. **Konventionsentscheid, unverändert.** 553 nackte gegen 261 beschriftete Stufenlabels (68 %; Runde 1: 62 %); 339 von 339 Rationale-Blöcke vollständig; der heute korrigierte Evidence-Generator akzeptiert beide Formen.

### 🔴 Critical

Nichts zu berichten: kein Kommentar beschreibt entfernte Parameter oder Klassen, keine Dokumentation stellt eine bekannte Fehlfunktion als gewollt dar, keine öffentliche Deklaration mit Missbrauchspotenzial ist undokumentiert. Die beiden High-Befunde sind falsche Aussagen über NACHBARN (den anderen Twin, das Schwesterprojekt), nicht über den Code unter ihnen.

### 🟠 High

- [x] 17. [`./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/AmbientContextRestorer.kt:19-20`; dieselbe Aussage in öffentlicher KDoc `./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/ClientRequestLoggingFilter.kt:46-47`] {High} {Confidence: high} {📚🟡} {Drift & staleness / Symmetrie-Aussage} "The RestClient twin has no counterpart … This is a deliberate stack difference, like `cancelled`" - das Gegenstück existiert seit demselben Tag
  - Finding: Geschrieben in Commit `4019201` (ADR-0010, 2026-09-16 19:22); Commit `5a9da1c` (ADR-0011, dieselbe Minute) führte `CallerMdcSnapshot`/`CallerMdcRestorer` im RestClient-Twin ein, amendierte ADR-0010 ("this section originally said the blocking twin gets no counterpart at all") und aktualisierte die Differenztabelle des README - nicht aber diese beiden Kommentare. Die öffentliche Filter-Bullet "where the blocking twin simply logs on the caller's thread" ist dieselbe Vereinfachung, die ein Dokka-Leser jetzt erhält; der RestClient-Twin nennt sich in `CallerMdcSnapshot.kt:22-23` korrekt "the RestClient counterpart of the WebClient twin's `AmbientContextRestorer`".
  - Information class: Why-not (Stack-Differenz)
  - Drift test: ja - eingetreten; kein Werkzeug vergleicht die KDoc eines Twins mit dem Code des anderen
  - Actionability: ja - wer die Schichtung ändert, sucht das Gegenstück des anderen Twins nicht, wenn die KDoc sagt, es gebe keines
  - Carrier strategy: Referenz - der Satz nennt "gleiche Schichtung, andere Quelle (der aufrufende Thread, ADR-0011)" und verweist auf ADR-0011; die Filter-Bullet erhält denselben Halbsatz. Kein Kürzen.
  - Residual information: "the blocking twin restores from a thread snapshot instead (ADR-0011): same layering, different source"
  - Target location: `./docs/adr/ADR-0010-reactive-twin-restores-the-callers-context.md` (amendierter Abschnitt "A deliberate stack difference"), `./docs/adr/ADR-0011-blocking-twin-snapshots-the-callers-mdc.md`
  - Pass: außerhalb der Pässe - Zwei-Zeilen-Korrektur, sofort (wie Runde-1-Befund 1)

- [x] 18. [`./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/CorrelationHeader.kt:15-16`; derselbe Satz in `./docs/adr/ADR-0002-trace-id-is-the-request-id.md:86-87` (außerhalb des Scopes, als Referenzziel relevant)] {High} {Confidence: high} {📚🟡} {Drift & staleness / Referenzqualität - Cross-Repo} "the sibling project limesium mirrors the rule on the inbound side so the pair stays consistent" - falsch seit dem 2026-09-05
  - Finding: Legatium akzeptiert Korrelations-IDs bis `MAX_LENGTH = 200` (`:19`, Commit `72b277d`, 2026-09-04). Limesium senkte seine Grenze auf `CorrelationHeaderValue.MAX_LENGTH = 128` (limesium-Commit `a9f992c`, 2026-09-05 15:20 - nach diesem Kommentar und nach Runde 1). Eine propagierte ID von 129-200 Zeichen wird von legatium akzeptiert und GESENDET, von einem limesium-Peer als `generated` ersetzt - das Paar ist genau dort inkonsistent, wo der Satz Konsistenz verspricht. Keines der beiden Repositories pinnt die Grenze als Literal (beide Tests verwenden die Konstante: `CorrelationHeaderTest.kt:17,35`, limesium `CorrelationHeaderValueTest.kt:24,44`) - anders als beim Fingerprint gibt es keinen Cross-Repo-Pin. Überlappung mit der Defekt-Analyse: ob 128 gegen 200 ein Defekt ist, wird hier nicht verfolgt; Runde 1 hatte diese Stelle als Low (Befund 12) geführt.
  - Information class: Why (Vertrag mit dem Schwesterprojekt)
  - Drift test: ja - eingetreten; kein Build, Test oder Dokka-Lauf sieht die Konstante eines anderen Repositories
  - Actionability: ja - wer `MAX_LENGTH` ändert, braucht die Grenze des Peers; heute nennt der Satz ihm eine falsche
  - Carrier strategy: Referenz + Test - der lokale Satz behält die Entscheidung ("bounded like the URI; treated as ABSENT outside the rule - ADR-0002") und lässt die Konsistenzbehauptung fallen; die Cross-Repo-Beziehung lebt einmal in ADR-0002 (das ein Amendment mit den tatsächlichen Grenzen braucht - außerhalb dieses Moduls); der tragfähige Träger ist das Fingerprint-Modell: ein identisches Literal, in den Tests beider Repositories gepinnt.
  - Residual information: "at most [MAX_LENGTH] visible-ASCII characters; outside the rule treated as ABSENT (ADR-0002)"
  - Target location: `./docs/adr/ADR-0002-trace-id-is-the-request-id.md` (Amendment-Abschnitt); `./legatium-common/src/test/kotlin/eu/inqudium/legatium/common/CorrelationHeaderTest.kt` (Literal-Pin)
  - Pass: 3 für den Kommentar (nach dem ADR-Amendment); 4 für den Literal-Pin

### 🟡 Medium

- [x] 19. [`./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/ClientRequestLoggingFilter.kt:29-48` (Liste "Stack-inherent differences … all deliberate"); zweite Kopie `./legatium-webclient-logging/README.md`, Tabelle "Deliberate stack differences" (außerhalb des Scopes)] {Medium} {Confidence: high für den Breadcrumb, medium für die Retry-ID} {📚🟠} {API documentation - sharpen / Symmetrie-Aussage} Die öffentliche Liste der bewussten Stack-Differenzen ist unvollständig gegen die Erklärungen des anderen Twins
  - Finding: (a) Die RestClient-KDoc (`./legatium-restclient-logging/src/main/kotlin/eu/inqudium/legatium/restclient/logging/ClientRequestLoggingInterceptor.kt:56-60`, heute korrigiert) erklärt den WARN-Breadcrumb beim geworfenen Call zur "deliberate difference from the reactive twin, which reports a failed call through the exchange event alone"; der geteilte Guide kennt sie (`./docs/GUIDE.md:318` "(blocking stack)"); die Liste des Filters und die README-Tabelle nicht. Der Code bestätigt die Asymmetrie: der `catch (e: Exception)` des Filters (`:141-144`) leitet in `Mono.error` ohne Breadcrumb; nur `abandonExchange` (Error-Pfad) schreibt einen, wie im Twin. (b) Der RestClient-Twin merkt sich seine generierte ID über Re-Entries eines retrying Interceptors (`GENERATED_ID_ATTRIBUTE`, `ClientIdentity.resolve(generatedEarlier = …)`); der Filter hat keinen solchen Mechanismus und löst die Identität bei jeder Subscription aus dem unveränderlichen Caller-Request (`:128-129`), ein traceless Retry erhält mit dem zählenden Default je Versuch eine neue ID. Der WebClient-Guide behauptet das Gegenteil (`./legatium-webclient-logging/docs/GUIDE.md:936-938`); kein Test pinnt eine der beiden Richtungen. (b) ist eine Verhaltens-/Guide-Frage - Überlappung mit der Defekt-Analyse, notiert, nicht verfolgt; sie gehört auf die Liste, als Differenz oder nach einem Fix.
  - Information class: Contract (öffentliche API)
  - Drift test: ja - die Liste ist Prosa; die Twins divergierten, ohne dass etwas rot wurde
  - Actionability: ja - ein Consumer mit beiden Twins liest genau diese Liste
  - Carrier strategy: schärfen - je eine Bullet pro Differenz in der öffentlichen Liste (Breadcrumb; Retry-Identität, sobald entschieden), die README-Tabelle als zweite Kopie; die RestClient-KDoc behält ihre Seite.
  - Residual information: die Liste selbst
  - Target location: diese Klassen-KDoc (existiert); `./legatium-webclient-logging/README.md`
  - Pass: 2 (die Liste ist der kanonische Ort für Differenzen); (b) nachdem die Defekt-Analyse entschieden hat

- [x] 20. [`./legatium-restclient-logging/src/main/kotlin/eu/inqudium/legatium/restclient/logging/ClientRequestLoggingInterceptor.kt:62-68` (Abschnitt "MDC coverage")] {Medium} {Confidence: medium} {📚🟡} {API documentation - sharpen} Der Abschnitt der öffentlichen Entry-Point-KDoc stammt von vor ADR-0011 und schweigt über den Caller-MDC-Snapshot
  - Finding: Der Abschnitt (`git blame` 2026-09-03/04) verspricht "the client line joins the server line by MDC alone" und endet mit "the body read and the emission happen after the interceptor returned, under the emission's own scope". Seit `5a9da1c` restauriert die Emission auf einem ANDEREN Thread zusätzlich den Snapshot der Caller-MDC; der Dokka-Leser erfährt das nur aus der `internal` KDoc von `CallerMdcSnapshot` oder aus dem Modul-Guide §2.6. Die KDoc ist nicht falsch, aber unvollständig - und der Entry Point muss ohne Sprung in `internal` tragen.
  - Information class: Contract
  - Drift test: nein (unvollständig, nicht falsch; Verhalten gepinnt durch `ClientRequestLoggingInterceptorTest` "… closed on another thread")
  - Actionability: ja - ein Host, der eine Response an einen Worker-Thread gibt, liest genau diesen Abschnitt
  - Carrier strategy: ein Satz im Abschnitt (Entscheidung + ADR-0011-Nummer); kein Kürzen.
  - Residual information: entfällt (Schärfung)
  - Target location: die Klassen-KDoc selbst; kanonisch ADR-0011, `./legatium-restclient-logging/docs/GUIDE.md` §2.6
  - Pass: 2

- [x] 21. [`./legatium-restclient-logging/src/main/kotlin/eu/inqudium/legatium/restclient/logging/ExchangeLogEmitter.kt:92, 151, 271`; `…/ClientRequestLoggingInterceptor.kt:122, 147, 344, 376`; `./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/ExchangeLogEmitter.kt:85-86, 138, 287`; `…/ClientRequestLoggingFilter.kt:134, 146, 153, 319`; `./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/ClientActivation.kt:28`] {Medium} {Confidence: high} {📚🟡} {Referenzqualität - Scheinabsicherung} `[Symbol]`-Links in `//`-Zeilenkommentaren sehen Dokka-geprüft aus und sind es nicht
  - Finding: 11 bis 15 `[…]`-Links stehen in Zeilenkommentaren (`[withEmissionScopes]`, `[ClientLoggingProperties.slowRequestThreshold]`, `[ClientLoggingMetrics.eventEmitted]`, `[ExchangeLogEmitter.logRequestStart]`, `[failOpen]`, `[ExchangeLogEmitter]`, `[ClientLoggingMetrics.requestId]`, `[ObservedResponse]`, `[tee]`, …). Runde 1 zählte 0 und hielt fest, dass Dokka nur KDoc prüft; die Fixes vom 2026-09-05 (Runde-1-Befunde 3, 5, 14) und die heutige `withEmissionScopes`-Extraktion haben sie erzeugt. Alle lösen heute auf (`failOpen` ist im Interceptor nicht einmal importiert - in KDoc würde der Link bereits fehlschlagen). Die CONTRIBUTING-Regel "link production symbols with `[Symbol]`" ist damit der Form nach erfüllt, der Sache nach nicht.
  - Information class: Referenz (Träger für Why)
  - Drift test: ja - ein Rename lässt den geklammerten Namen mit der Autorität eines geprüften Links stehen
  - Actionability: ja - der Leser folgt ihm, um den Guard zu prüfen
  - Carrier strategy: wo der Satz die nächste Anweisung begründet, bleibt er inline, trägt aber ohne Sprung (Backticks, keine Klammern); wo er eine Regel einer anderen Deklaration nennt, wandert der Satz in die KDoc der umschließenden Funktion, wo Dokka ihn prüft. Guardrail (Pass 5): ein CI-`grep` auf `^\s*//.*\[[A-Za-z]\w*(\.\w+)?\]` → 0.
  - Residual information: unverändert
  - Target location: die KDocs der umschließenden Funktionen; Abbruchkriterium "symbolische Links in `//`-Kommentaren → 0"
  - Pass: 3 (Guardrail 5)

- [x] 22. [`./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/ExchangeLogEmitter.kt:105-109`] {Medium} {Confidence: high} {📚🟡} {Drift & staleness / Referenzqualität} "called exactly once per exchange from `ClientRequestLoggingFilter.complete` - the one place that wins the [Exchange.state] transition" nennt einen von zwei Gewinnern
  - Finding: Die KDoc stammt vom 2026-09-03 (`8f7c66f`). Seit `7aa90eb` (2026-09-05) gewinnt auch `cancelUnlessResponded` (`ClientRequestLoggingFilter.kt:252-264`) die Transition über seine CAS-Schleife; beide Pfade münden in `finish` (`:267-270`, heute extrahiert), das `logExchange` aufruft. "The one place" ist falsch; die Exactly-once-Eigenschaft gilt weiter. Die Methode steht zudem in Backticks, wo `[ClientRequestLoggingFilter.complete]` (`internal`, Dokka-geprüft - vgl. `[ExchangeLogEmitter.logRequestStart]` in `ClientRequestLoggingFilter.kt:134`) ein geprüfter Verweis wäre; dieselbe Backtick-Form in `ObservedBody.kt:15` und `ObservedResponse.kt:43` (dort inhaltlich korrekt).
  - Information class: Contract (intern: der Emitter vertraut dem Exactly-once des Aufrufers)
  - Drift test: ja - eingetreten
  - Actionability: ja - wer einen Terminalpfad hinzufügt, muss wissen, dass der Guard stromaufwärts liegt und wo
  - Carrier strategy: symbolische Referenz - die Exactly-once-Transition benennen statt einer Methode, mit `[ClientRequestLoggingFilter.complete]` (und dem Cancel-Pfad) als geprüften Links; `finish` ist privat und nicht verlinkbar.
  - Residual information: "called once per exchange, after the filter won the [Exchange.state] transition to `COMPLETED`"
  - Target location: KDocs von `ClientRequestLoggingFilter.complete`/`cancelUnlessResponded` (existieren)
  - Pass: 3

- [x] 23. [`./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/BodyCapture.kt:21-25`] {Medium} {Confidence: medium} {📚🟠} {Drift & staleness / Trägerschaft (Test)} Die heute geschriebene Read-State-KDoc behauptet ein framework-internes Verhalten als gezählten Zustand, den kein Test pinnt
  - Finding: Geschrieben in `4fbc7b1`: "the body skip of `bodyToMono(Void.class)` cancels after the first buffer and counts as [PARTIAL]". `ClientRequestLoggingFilterTest.kt:435-453` pinnt das OUTCOME dieses Idioms (`success`, INFO), asserted aber kein `state=partial`-Tag; die `partial`-Pins (`:428`, `:463`, `ClientRequestLoggingFilterBodyAndHeaderTest.kt:463`) sind die Out-of-band-Cancel-Fälle. Die beiden anderen Aussagen des Absatzes SIND gepinnt (`releaseBody()`/`toBodilessEntity()` → complete mit Bytes: WebClient-`ClientRequestLoggingMetricsTest.kt:239-252`; blockierendes `toBodilessEntity()` → unread: `ClientRequestLoggingInterceptorBodyAndHeaderTest.kt:391-406`). Das Verhalten beruht auf Springs `takeWhile(release; false)`-Interna (`ObservedBody.kt:23` dokumentiert dasselbe) - genau das, was ein Spring-Upgrade ändert, ohne dass hier etwas rot wird, während diese KDoc die operator-facing Definition des `state`-Tags ist.
  - Information class: Contract (Tag-Semantik) + How (Spring-Interna)
  - Drift test: ja - ein Framework-Wechsel kippt den gezählten Zustand still
  - Actionability: ja - ein Operator liest `partial` auf einer gesunden Route und muss wissen, dass der Void-Skip dort landet
  - Carrier strategy: Test - eine Assertion auf `adapter.response.body.read{state=partial}` für den Void-Skip neben dem bestehenden `toBodilessEntity → complete`-Fall; der Satz bleibt danach als gepinnter Vertrag. Der Backtick `` `BoundedBodyCapture` `` (`:19`) ist legitim - common kann nicht in die Twins verlinken.
  - Residual information: der ganze Absatz (Vertrag)
  - Target location: `./legatium-webclient-logging/src/test/kotlin/eu/inqudium/legatium/webclient/logging/ClientRequestLoggingMetricsTest.kt`
  - Pass: 4

- [x] 24. [`./legatium-restclient-logging/src/main/kotlin/eu/inqudium/legatium/restclient/logging/BoundedBodyCapture.kt:31-38, 96-101` ↔ `…/CapturingClientHttpResponse.kt:23-29` ↔ `…/ClientRequestLoggingInterceptor.kt:236-240, 267-270` ↔ kanonisch (No-Body-Hälfte) `./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/BodyCapture.kt:14-19`] {Medium} {Confidence: high} {📚🟡} {Lossless condensation - Wiederholung entlang einer Aufrufkette} Die Content-Length-/No-Body-Vollständigkeitsregel steht fünfmal entlang einer Aufrufkette
  - Finding: "a reader that knows the length asks for exactly that many bytes and never for the EOF (`ByteArrayHttpMessageConverter`)" und "a 1xx/204/304 answer has no body by the protocol, the clients never open it, it counts complete" werden an der Klassen-KDoc der Capture, an `expectBytes`, an der Klassen-KDoc des Response-Wrappers, an `snapshotResponse` und an `declaredBodyLength` wiederholt; vier der fünf Stellen sind von heute (`4fbc7b1`, Defekt-Befund 1). Nur die Wrapper-KDoc trägt das Fehlerbild ("without it every `byte[]` answer counted as `partial`" - die in Runde 1 geschützte Chronik) und nur `declaredBodyLength` nennt Springs `hasMessageBody()`-Regel (verifiziert gegen spring-web 7.0.9: 1xx/204/304, dann `getContentLength() != 0`). Die Tests pinnen das Verhalten (`ClientRequestLoggingMetricsTest` "… bodiless 204 …", "… ByteArray converter reads to its declared length …"), nicht die Prosa.
  - Information class: Why / Why-not
  - Drift test: ja - fünf Kopien; eine Regeländerung (etwa für HEAD-Antworten) muss an fünf Stellen nachgezogen werden, ohne dass etwas rot wird
  - Actionability: ja an `declaredBodyLength` (Entscheidungspunkt) und am Wrapper (Beobachtungspunkte); an `snapshotResponse` und `expectBytes` genügt ein Halbsatz
  - Carrier strategy: eine kanonische Aussage - die Klassen-KDoc von `CapturingClientHttpResponse` (sie besitzt die Beobachtungspunkte und das Fehlerbild) - und `[BodyReadState]` für die No-Body-Hälfte; die drei anderen Stellen behalten Entscheidung + `[Symbol]`-Link (`[BoundedBodyCapture.expectBytes]`, `[declaredBodyLength]`), in KDoc-Form (Befund 21).
  - Residual information: an `declaredBodyLength`: null für 1xx/204/304 (Springs `hasMessageBody`-Regel), `Content-Length` wenn vertrauenswürdig, sonst unbekannt; "PEER-CONTROLLED input feeds only the comparison" bleibt wörtlich (📚🔴). An `expectBytes`: "only a length the caller trusts; zero completes right here".
  - Target location: Klassen-KDoc `CapturingClientHttpResponse` (existiert); `BodyReadState`-KDoc (existiert)
  - Pass: 3 (≈ −10 bis −14 Zeilen)

- [x] 25. [`./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/Exchange.kt:55-63` ↔ `:89-94` ↔ `:96-106`] {Medium} {Confidence: high} {📚🟡} {Struktur & Lokalität / Lossless condensation - zweite Wahrheit} Die Zustandsmaschine ist dreimal in einer Datei ausgeschrieben
  - Finding: Die `state`-Property-KDoc zählt alle vier Zustände mit ihren Übergängen auf (9 Zeilen, 2026-09-05); die `ExchangeState`-Klassen-KDoc wiederholt `DELIVERING` und `RESPONDED` (6 Zeilen); seit heute (`c30c14c`, Code-Style-Befund 22) trägt jeder Enum-Eintrag seine eigene KDoc mit denselben Worten. Ein fünfter Zustand würde an den Einträgen hinzugefügt; die beiden Prosa-Listen folgen nicht. Nichts erzwingt sie (kein erschöpfendes `when` über `ExchangeState` - nur CAS-Vergleiche), die Einträge sind die nächste Wahrheit. Die Übergangs-Prosa in `ObservedResponse.kt:26-34` und `ClientRequestLoggingFilter.kt:244-250` ist ortsgebunden (sie begründet den CAS-Code darunter) und NICHT Teil dieses Befunds.
  - Information class: How (Übergänge) + Why (warum `DELIVERING` existiert, warum ein ungelesener Body offen bleibt)
  - Drift test: ja - drei Prosa-Kopien, keine geprüft
  - Actionability: ja an den Einträgen und für die zwei Why-Sätze; nein für die wiederholte Aufzählung
  - Carrier strategy: Kondensation - die Einträge tragen die Bedeutung; die `state`-KDoc behält einen Satz (ein atomarer Wert, Exactly-once-Transition) + `[ExchangeState]`; die `ExchangeState`-Klassen-KDoc behält nur die zwei Why-Sätze (nie subscribter Body bleibt auf dem Gauge; `DELIVERING` existiert für die Race → `[ObservedResponse]`).
  - Residual information: die zwei Why-Sätze
  - Target location: `ExchangeState`-Einträge (existieren)
  - Pass: 3 (erwartete Einsparung 8-10 Zeilen)

- [x] 26. [`./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/ClientLoggingMetrics.kt:126-129` ↔ `:386-393`; `:143-145` ↔ `:396-401`; `:159-163` ↔ `:420-427`; `:182-184` ↔ `:429-434`] {Medium} {Confidence: high} {📚🟡} {Lossless condensation - Wiederholung innerhalb einer Datei} Der Zweck jedes vorregistrierten Meters steht zweimal: über dem Feld und an der `*_METER`-Konstante
  - Finding: Der `//`-Block über `failOpenCounters` ("the missing exchange line IS the symptom … A counter travels the independent metrics channel") und die KDoc von `FAIL_OPEN_METER` ("makes its price visible on a channel independent of the possibly-broken log pipeline") sagen dasselbe; ebenso `eventCounters` ↔ `EVENTS_METER` (nahezu wörtlich: "after the level gate, arrival lines excluded … ground truth to reconcile against the log index … loss in the log pipeline"), `openExchanges` ↔ `OPEN_EXCHANGES_METER` ("loses the event SILENTLY … the one failure mode neither … can see"), `requestIdSourceCounters` ↔ `CORRELATION_METER` ("a rising `generated` share means the application stopped propagating"). Dieselbe Gestalt wie Runde-1-Befund 7 (behoben), eine Ebene tiefer. Die Konstanten sind die Namen, die `SharedContractTest` pinnt und auf die die Twin-Tests verlinken - die kanonische Kopie.
  - Information class: Why
  - Drift test: nein für die Fakten (Micrometer-Semantik, gepinnt durch `ClientLoggingMetricsTest`); ja für das Auseinanderlaufen der Kopien (das Events-Paar unterscheidet sich bereits im Wortlaut)
  - Actionability: ja an der Konstante; am Feld genügt eine Klausel ("pre-registered per stage - [FAIL_OPEN_METER]")
  - Carrier strategy: symbolische Referenz - jeder Feldkommentar schrumpft auf eine Zeile mit dem Dokka-geprüften `[FAIL_OPEN_METER]`/`[EVENTS_METER]`/`[OPEN_EXCHANGES_METER]`/`[CORRELATION_METER]`-Link; die Begründung bleibt einmal an der Konstante. Was am Feld stack-spezifisch ist (der `taken`-Guard, der Per-Twin-Tag-Kommentar `:173-175`), bleibt.
  - Residual information: eine Klausel je Feld, die benennt, was registriert wird
  - Target location: die KDocs der vier Konstanten (existieren)
  - Pass: 3 (≈ 13 Zeilen)

- [x] 27. [`./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/HeaderLogProperties.kt:13-26` ↔ `:32-39`] {Medium} {Confidence: high} {📚🟡} {Documentation duplication - innerhalb einer öffentlichen Klasse} Der Fix von Runde-1-Befund 6 kopierte die vier Listen-Semantiken auf die Properties und ließ die Klassen-KDoc-Bullets ungekürzt
  - Finding: Die Bullets `includes/excludes/masked/unmasked` (14 Zeilen) und die vier Property-KDocs (4 Zeilen à 154-172 Zeichen) formulieren dieselben Regeln ("empty means NONE", "exclude always wins", "`*` rejected at binding", "unmasked always wins"); Dokka rendert beide auf einer Seite. Zwei Kopien eines öffentlichen Vertrags altern getrennt. Öffentliche API - die Empfehlung entfernt eine zweite Kopie, keine Aussage.
  - Information class: Contract
  - Drift test: nein für die Fakten (`HeaderLogPropertiesTest`, `HeaderMaskingFuzzTest` pinnen jeden Rückweisungsfall); ja für das Auseinanderlaufen der Kopien
  - Actionability: ja an der Property (IDE-Quick-Doc, Dokka-Property-Seite); die Klassen-KDoc braucht das Zusammenspiel, nicht die Regel pro Liste
  - Carrier strategy: Property-KDoc ist kanonisch (dort schärfen: die Begründung "warum `*` in excludes abgelehnt wird" lebt derzeit nur in der Klassen-Bullet `:16-18` und gehört an die Property); die Klassen-KDoc behält `:4-11` (Zweck, masked-by-default, ADR-0005) und `:28-29` (Maskierung betrifft nur geloggte Header; case-insensitive) plus einen Satz zur Präzedenz (exclude > include, unmasked > masked).
  - Residual information: der Präzedenzsatz in der Klassen-KDoc
  - Target location: Property-KDoc `:32-39` (existiert)
  - Pass: 3 (≈ 10 Zeilen netto; öffentliche API - Review durch einen Leser der gerenderten Dokka-Seite)

### 🟢 Low

- [x] 28. [`./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/Exchange.kt:18-43` ↔ `./legatium-restclient-logging/src/main/kotlin/eu/inqudium/legatium/restclient/logging/Exchange.kt:18-27,33-43`; `…/ClientRequestLoggingFilter.kt:326-328` ↔ `…/ClientRequestLoggingInterceptor.kt:404-406`] {Low} {Confidence: high} {📚🟡} {Documentation duplication - Twin, Rest von Runde-1-Befund 3} 10 Feld-KDoc-Zeilen und die `newCaptures`-Regel wortgleich in beiden Twins, obwohl kanonische KDoc in common existiert (`RequestTarget`, `ClientIdentity`, `TraceMdcKeys`, `AdapterName`, `DeclaredCharset`; `BodyLogMode.captures` + `ClientLoggingProperties.measureRequestBodySize`). Trägerstrategie: je eine Phrase + `[Symbol]`-Link pro Feld; `newCaptures` → `[BodyLogMode.captures]` + `[ClientLoggingProperties.measureRequestBodySize]`. Der `captureFor`/`newCaptures`-CODE ist byte-identisch in beiden Twins - ein Pass-4-Extraktionskandidat wie `declaredCharsetOrUtf8` (Überlappung mit der Architektur-/Defekt-Spur). Ziel: die genannten Common-KDocs. Pass 3 (≈ 5 Zeilen je Twin); Code Pass 4.

- [x] 29. [`./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/ExchangeLogEmitter.kt:51-55, 157-165, 183-187, 209-213`, `…/Exchange.kt:47-53`, `…/ClientRequestLoggingFilter.kt:126-127`; Spiegel für ADR-0011 in `./legatium-restclient-logging/src/main/kotlin/eu/inqudium/legatium/restclient/logging/ExchangeLogEmitter.kt:53-57, 169-177, 195-198, 321-325`, `…/ClientRequestLoggingInterceptor.kt:204-207`] {Low} {Confidence: medium} {📚🟡 für die Schichtung, 📚🟠 für die Teardown-Regel} {Documentation duplication - Twin, mit kanonischem ADR} Die ADR-0010-Schichtung ist in fünf KDocs des reaktiven Twins paraphrasiert (≈ 22 Zeilen), der RestClient-Twin trägt den ADR-0011-Spiegel. Die `restoreQuietly`-KDoc (`:184-187`) nennt "the RestClient twin's rule" - ein Cross-Twin-Prosa-Verweis auf eine Regel (Teardown nach der Zeile auf dem Logger ist Buchhaltung, nie eine verlorene Zeile, maskiert nie die Exception des try), die KEINEN kanonischen Ort hat: sie steht in `Interceptor:204-207`, RestClient-`Emitter:322-325` und hier; die `failOpen`-KDoc (`./legatium-common/…/FailOpenDiagnostics.kt:30-33`) listet Guards mit reicherer Semantik, aber nicht diese. Trägerstrategie: `withEmissionScopes` behält Entscheidung + ADR-0010/0011; die Teardown-Regel braucht zuerst einen Anker (Kandidat: die `failOpen`/`reportQuietly`-KDoc in `FailOpenDiagnostics.kt` oder ein "Fail-open"-Abschnitt in ADR-0010/0011), dann werden die Twin-Kopien je ein Satz. Residual: "ambient/caller scope outside, [MdcScope] inside (ADR-0010/0011); teardown failures are bookkeeping (stage=wiring), never a lost line". Ziel: ADR-0010, ADR-0011 (existieren); Teardown-Regel: in `FailOpenDiagnostics.kt` zu schaffen (Pass 2). Pass 2, dann 3 (≈ 6-8 Zeilen je Twin nach dem Anker).

- [x] 30. [`./legatium-restclient-logging/src/main/kotlin/eu/inqudium/legatium/restclient/logging/ExchangeLogEmitter.kt:34-40` ("## Levels") ↔ `:216-221` (`classify`); `./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/ExchangeLogEmitter.kt:33-38` ↔ `:231-234`; kanonisch `./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/Classification.kt:5-8`, `ClientLogFields.kt:45-51`] {Low} {Confidence: high} {📚🟡} {Lossless condensation - Wiederholung innerhalb einer Datei, beide Twins} Die Level/Outcome-Matrix steht je Emitter zweimal (Klassen-KDoc und `classify`) und zweimal kanonisch in common; der `when` in `classify` ist die dritte Kopie. Die Per-Branch-Gründe ("the peer is slow, not broken", "the peer answered, the application decides") existieren nur an `classify` (RestClient) und bleiben (📚🔴-Rest); der reaktive Twin ergänzt `cancelled`. `:217-218` (RestClient) nennt `ClientLogField.OUTCOME` in Prosa, obwohl importiert und verlinkbar. Trägerstrategie: Klassen-KDoc behält Prinzip + Kostensatz ("resolved BEFORE the event is built") + `[classify]`; die Branch-Liste lebt einmal an `classify`, mit `[ClientLogField.OUTCOME]`. Gepinnt durch die Level/Outcome-Matrix der Interceptor-/Filter-Tests. Pass 3 (≈ −3 Zeilen je Twin).

- [x] 31. [`./legatium-restclient-logging/src/main/kotlin/eu/inqudium/legatium/restclient/logging/ClientLoggingAutoConfiguration.kt:78-80` ↔ `:105-110`; `./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/ClientLoggingAutoConfiguration.kt:68-75` ↔ `:85-95`] {Low} {Confidence: high} {📚🟡} {Lossless condensation - Wiederholung innerhalb einer Datei (öffentliche API) / Referenzqualität} Die Konsequenz "unordered host customizer = `LOWEST_PRECEDENCE`, applied after, runs inside" steht je Auto-Konfiguration zweimal (beide Blöcke von heute, `4fbc7b1`, Defekt-Befund 13), und "see the constant" steht zwei Zeilen unter einem geprüften `[CUSTOMIZER_ORDER]`-Link. Gepinnt durch `ClientLoggingAutoConfigurationTest:85` (RestClient) bzw. `:73` (WebClient). Trägerstrategie: die volle Regel einmal an `CUSTOMIZER_ORDER` (von Dokka gerendert, dort wählt ein Host sein `@Order`); die verschachtelte Konfiguration behält eine Klausel + den bestehenden Link; "see the constant" fällt. Residual: "an unordered host customizer runs inside the logging ([CUSTOMIZER_ORDER])". Pass 3 (≈ −2 bis −4 Zeilen je Twin).

- [x] 32. [`./legatium-restclient-logging/src/main/kotlin/eu/inqudium/legatium/restclient/logging/ClientRequestLoggingInterceptor.kt:341-344` ↔ `…/ExchangeLogEmitter.kt:353-358` ↔ kanonisch `./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/ClientLoggingProperties.kt:92-95`] {Low} {Confidence: high} {📚🟡} {Lossless condensation - Doppelbegründung / Lokalität} Die Regel "request size sample needs a response" wird dreimal hergeleitet: an der Property (kanonisch, inkl. "the reactive stack needs no such rule"), an der `recordBodySizes`-KDoc des Emitters (Entscheidungspunkt, 6 Zeilen) und im Inline-Block von `wireExchange`. Der erste Satz des Inline-Blocks begründet die nächste Anweisung (copy BEFORE the wire call) und bleibt; seine zweite Hälfte ("the field documents it as exactly that; the size meter records it only once a response proves the request went out") ist Konzeptwissen einer anderen Klasse - der einzige Lokalitätskandidat in `wireExchange` mit Konzeptinhalt, und das Ziel existiert, also Kondensation, keine Dekomposition. Trägerstrategie: Interceptor behält den ortsgebundenen Satz + `[ClientLoggingProperties.measureRequestBodySize]` in KDoc-Form; die Emitter-KDoc behält das Seam-Argument als Entscheidungspunkt; die Property bleibt kanonisch. Residual: "copied BEFORE the wire call - what the client is about to send, not what reached the peer". Pass 3 (≈ −2 Zeilen).

- [x] 33. [`./legatium-restclient-logging/src/main/kotlin/eu/inqudium/legatium/restclient/logging/CallerMdcSnapshot.kt:14-20` ↔ `:31-35`; `…/ClientRequestLoggingInterceptor.kt:72-75` ↔ `:157-160`] {Low} {Confidence: medium} {📚🟡} {Struktur & Lokalität - Klassenregel am Member wiederholt} `restore` wiederholt Thread-Regel und Additivität der Klassen-KDoc (beide von heute; kanonisch ADR-0011 "Restoration"); `wireOrNull` wiederholt den Abschnitt "Fail-open, including the wiring" der öffentlichen Klassen-KDoc (identische Aufzählung: identity resolution, header selection, capture construction / time source; `stage=wiring`; pass-through). Beide gepinnt (`CallerMdcSnapshotTest`, Metrics-Test "… count stage wiring when the id generator throws"). Trägerstrategie: Member behält eine Klausel + `[ClassName]`/Abschnittshinweis; die Klassen-KDoc (öffentlich, Dokka) bleibt der kanonische Text. Residual: `restore`: "no-op on the capturing thread and for an empty snapshot"; `wireOrNull`: "fail-open like the emission: null = pass-through, counted `stage=wiring`". Pass 3 (≈ −5 Zeilen).

- [x] 34. [`./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/ObservedBody.kt:39-49`] {Low} {Confidence: medium} {📚🟡} {Documentation duplication - Code ↔ Guide} "The limit of the heuristic" (11 Zeilen, heute hinzugefügt, Defekt-Befund 10) ist wortgleich der Guide-Absatz (`./legatium-webclient-logging/docs/GUIDE.md:838-850`) und sagt das selbst ("documented in the guide"). Zwei ungeprüfte Kopien eines consumer-facing Vorbehalts. Der ortsgebundene Teil - die Regel liest WO, nicht WARUM, und die zwei akzeptierten Fehldeutungen (Hop → falsches `cancelled`; Limit innerhalb der Delivery → `success`/`partial`) - muss am Code bleiben: wer `deliveringOn` ändert, braucht ihn. Herleitung und Beispiele sind Sache des Guides. Trägerstrategie: Kondensation - Entscheidung + die zwei Fälle in 3-4 Zeilen mit benanntem Guide-Abschnitt. Residual: "reads WHERE a cancel comes from, not WHY: a scheduler hop before an early-exiting consumer logs `cancelled`; a limit raised inside the delivery logs `success`/`partial` - accepted (guide §4.3)". Ziel: Guide §4.3 (existiert). Pass 3 (≈ 7 Zeilen) - KEINE Hygiene-Löschung.

- [x] 35. [`./legatium-restclient-logging/src/main/kotlin/eu/inqudium/legatium/restclient/logging/ExchangeLogEmitter.kt:29-30`; wortgleich `./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/ExchangeLogEmitter.kt:29-30`] {Low} {Confidence: medium} {📚⚪} {Drift & staleness - Textreferenz} "fields locked by `ClientLogFieldTest` … in both twins" - dieser Test existiert einmal, in `legatium-common` (seit ADR-0003, erstes Amendment, 2026-09-03; der Satz ist vom selben Tag, aus `8f7c66f`, und älter als der Umzug). "In both twins" gilt nur für den Message-Pin von `TwinContractTest`, wie dessen KDoc `:11-15` korrekt sagt. Beide Pins verifiziert. Trägerstrategie: umformulieren, sodass "in both twins" am Message-Pin hängt; ADR-0003 als Anker für die einmal gepinnten Felder. Pass 3.

- [x] 36. [`./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/BoundedBodyCapture.kt:21`] {Low} {Confidence: high} {📚🟡} {Drift & staleness} "a CANCEL … runs `doFinally` immediately" nennt einen Mechanismus, den das Modul nicht mehr verwendet: das `doFinally` des Filters wurde in `7aa90eb` (2026-09-05) durch das eigene `cancel()` von `[ObservedBody]` ersetzt; die Zeile ist vom 2026-09-03. Die Substanz (der Terminal-Callback läuft beim Cancel, während ein bereits angefordertes `onNext` noch eintreffen kann) gilt; der Name ist veraltet, `grep doFinally` findet im Modul nur Kommentare. Trägerstrategie: `[ObservedBody]` statt des Operatornamens. Pass 3.

- [x] 37. [`./legatium-restclient-logging/src/main/kotlin/eu/inqudium/legatium/restclient/logging/ClientRequestLoggingInterceptor.kt:93` ("see below"), `:184` (`MdcScope` in Prosa), `:414` ("by the rule above"); `…/Exchange.kt:33` (`` `AdapterName.ATTRIBUTE` `` in Backticks); `…/ExchangeLogEmitter.kt:218` ("(see ClientLogField.OUTCOME)"); `./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/ClientRequestLoggingFilter.kt:95` ("(see below)"), `:104` ("(see the class KDoc)"); `…/BoundedBodyCapture.kt:120` ("exposed for the tee tests")] {Low} {Confidence: high} {📚⚪} {Referenzqualität} Acht In-Modul-Verweise in Prosa oder Backticks, wo CONTRIBUTING `[Symbol]` vorschreibt oder der Verweis falsch zeigt: "(see below)" am `meterRegistry`-Parameter beider Entry Points (Fix von Runde-1-Befund 8) meint den Klassen-KDoc-Abschnitt "Manual wiring", der im Quelltext und in Dokkas Rendering OBEN steht; `:414` meint die `newCaptures`-KDoc; `MdcScope`, `AdapterName.ATTRIBUTE` und `ClientLogField.OUTCOME` sind importiert bzw. per FQN verlinkbar (wie `ExchangeLogEmitter.kt:284` es für `BodyLogMode` vormacht); "exposed for the tee tests" - der Konsument von `isFrozen` ist `BoundedBodyCaptureTest:42`, nicht die Decorator-Tests. Trägerstrategie: `[Member]`/FQN-Links (Dokka-geprüft); für KDoc-Abschnitte, für die kein Symbol existiert, den Abschnitt benennen oder den Verweis streichen, da der Klassen-KDoc-Abschnitt der kanonische Text ist. Pass 1 für die drei Ortsangaben, Pass 3 für die Links.

- [x] 38. [`./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/CorrelationIdGenerator.kt:24`; `:8` (Backtick-Produktionssymbol)] {Low} {Confidence: high} {📚🟡} {Referenzqualität - öffentliche KDoc} `DEFAULT` (öffentlich) verlinkt `[CountingCorrelationIdGenerator]` (`internal` - von Dokka aufgelöst, auf der öffentlichen Seite ohne Link gerendert) und weist an "see that class's documentation for the rationale and the format contract". Der kanonische Ort der Rationale ist ADR-0004, das die interne Klasse selbst nennt (`:34-35`); der Format-Vertrag steht bereits im selben Satz (`:20-23`). `:8` nennt `` `ClientLoggingProperties.correlationIdHeader` `` in Backticks statt `[…]`. Trägerstrategie: Referenz - die Anweisung durch die ADR-Nummer ersetzen; der Format-Satz bleibt (öffentliche API, schärfen); `:8` als Link. Pass 3.

- [x] 39. [`./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/CorrelationIdGenerator.kt:41-43` ↔ `:69`] {Low} {Confidence: high} {📚🟡} {Lossless condensation - Wiederholung innerhalb einer Datei} Der Breiten-Grenztest ist nach Runde-1-Befund 13 weiterhin zweimal genannt: Klassen-KDoc ("… the width-boundary test in `CountingCorrelationIdGeneratorTest` guards through the `counterStart` seam") und `COUNTER_WIDTH`-KDoc ("guarded by the width-boundary test (ADR-0004)"). Test existiert (`CountingCorrelationIdGeneratorTest.kt:205-217`). Trägerstrategie: einmal, an der Konstante, deren Breite er schützt; die Klassen-KDoc behält "both widths are load-bearing". Pass 3 (1-2 Zeilen).

- [x] 40. [`./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/ClientLoggingProperties.kt:139-143`] {Low} {Confidence: medium} {📚🔴} {Trägerschaft - Test statt Wartungsprosa} "so a new property is added here as well" ist eine Regel, die nur der Kommentar durchsetzt: die heute (`c30c14c`, Code-Style-Befund 17) umgeschriebene `toString`-KDoc sagt, der Renderer "deliberately mirrors the constructor parameter list"; `ClientLoggingPropertiesTest.kt:72-81` pinnt nur die Redaktion. Eine der Datenklasse hinzugefügte und hier vergessene Property verschwindet still aus jedem Properties-Dump. Die Why-not-Hälfte ("the generated data-class `toString` cannot be reused once overridden") ist tragend und bleibt. Trägerstrategie: Test - eine Assertion, dass jeder Primärkonstruktor-Parametername in `toString()` vorkommt (Reflection über die Datenklasse); der Satz verkürzt sich dann auf "mirrors the constructor list (pinned)". Residual: "the generated toString cannot be reused once overridden". Ziel: `./legatium-common/src/test/kotlin/eu/inqudium/legatium/common/ClientLoggingPropertiesTest.kt`. Pass 4.

- [x] 41. [`./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/AmbientContextRestorer.kt:22-27` gegen `:62`] {Low} {Confidence: medium} {📚🟡} {Struktur & Lokalität} Die Begründung für `clearMissing(false)` steht auf der Interface-KDoc, 40 Zeilen von der Builder-Zeile entfernt, die keinen Kommentar trägt (und in einer anderen Klasse, `ContextPropagationRestorer`). ADR-0010 §"Additive" ist kanonisch. Trägerstrategie: Einzeiler an der Builder-Zeile (Entscheidung + ADR-0010); die Interface-KDoc behält "purely additive". Zeilenneutral, Lokalitätsgewinn. Pass 3.

- [x] 42. [`./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/BoundedBodyCapture.kt:28-30, 10-11`; dieselbe Lücke `./legatium-restclient-logging/src/main/kotlin/eu/inqudium/legatium/restclient/logging/BoundedBodyCapture.kt`] {Low} {Confidence: medium} {📚⚪} {Trägerschaft - Vorbedingungsprosa} "With `maxBytes = 0` … COUNT-ONLY mode" definiert 0; ein negativer Wert ist still count-only (`room > 0`, `minOf`) und undokumentiert. Trägerstrategie: ausführbare Assertion - `require(maxBytes >= 0)` an der Klassengrenze (Produktion übergibt nie einen: `ClientLoggingProperties` verlangt `maxBodyBytes > 0`); die Prosa nennt danach den Modus, nicht den Bereich. Pass 4.

- [x] 43. [`./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/ClientLogFields.kt:9`; auch `./legatium-common/src/test/kotlin/eu/inqudium/legatium/common/ClientLogFieldTest.kt:36`, `./docs/elk/README.md:5`, `./docs/GUIDE.md:323`] {Low} {Confidence: medium} {📚⚪} {Lossless condensation - Zahl, die das Enum hält} "the same fourteen fields" ist ein Zahlwort an vier Prosa-Stellen; Enum und Lockstep-Mengengleichheitstest halten die Zahl erschöpfend, die Prosa prüft niemand - beim Hinzukommen von `NAME` (`e35a2b0`) musste sie viermal angepasst werden. Aktuell korrekt. Trägerstrategie: Name/Enum - "the same fields under the same names"; Zahl streichen. Pass 1.

- [x] 44. [Inverser Befund - öffentliche Deklarationen ohne eigene KDoc: `./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/HeaderLogProperties.kt:94` (`WILDCARD`); `BodyCapture.kt:32-38` (`BodyReadState.tagValue`, Einträge `UNREAD/PARTIAL/COMPLETE`); `Mdc.kt:15-17` (`MdcKeys.REQUEST_ID/REQUEST_METHOD/ROUTE`)] {Low} {Confidence: high} {📚🟠} {API documentation - sharpen} Öffentliche Konstanten und Enum-Einträge sind nur in der Prosa der umschließenden KDoc dokumentiert: `WILDCARD` ist ein öffentliches `const`, das ein Host referenzieren kann (das Fuzz-Ziel tut es), ohne KDoc; die drei `BodyReadState`-Einträge sind der `state`-Tag-Vertrag und nur als `[UNREAD]:`-Prosa der Klassen-KDoc erklärt (Dokka-Eintragsseiten leer - die Gestalt, die Runde-1-Befund 6 an `HeaderLogProperties` korrigiert hat); `MdcKeys.REQUEST_METHOD = "adapter_method"` weicht vom Feldnamen `adapter_request_method` ab - dokumentiert in `./docs/GUIDE.md:214` und den READMEs, nicht an der Konstante. Trägerstrategie: die vorhandenen Sätze auf die Deklarationen verschieben (kein neuer Inhalt); die Klassen-KDocs behalten Zweck und Zusammenspiel. Pass 2.

- [x] 45. [`./legatium-common/src/main/kotlin/eu/inqudium/legatium/common/HeaderLogProperties.kt:34,36,38` (154-172 Zeichen); `CorrelationIdGenerator.kt:15,48,51,54,69` (133-149); `BodyLogMode.kt:34` (141); `ClientLoggingMetrics.kt:383` (133); `RequestTarget.kt:47` (137); `./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/ExchangeLogEmitter.kt:249` (158), `:320` (136); `…/Exchange.kt:33` (140)] {Low} {Confidence: high} {📚⚪} {Zeremonie / Formatierung - Regression des Musters von Runde-1-Befund 9} Vierzehn Einzeiler-KDocs laufen auf 133-172 Zeichen (der Fix von Befund 6 schuf drei davon; `max_line_length = off`, ktlint bricht KDoc nicht um). `ExchangeLogEmitter.kt:249` (WebClient) behauptet zudem "identical to the RestClient twin", während die Zeile des Twins (`:233`) das nicht tut - eine einseitige Symmetrie-Aussage (P6). Trägerstrategie: umbrechen; die einseitige Klausel streichen oder auf beide Seiten setzen. Pass 1.

**📚🔴-Schutzstellen (verifizierte einzige Kopien tragenden Warum-Wissens; keine Befunde; kein Hygiene-Pass darf sie anfassen):**
- `./legatium-webclient-logging/src/main/kotlin/eu/inqudium/legatium/webclient/logging/ObservedResponse.kt:15-34` - das verworfene `map`/Peek-Operator-Design MIT Fehlerbild ("no event, the open-exchanges gauge one too high forever"); das Fehlerbild steht inzwischen auch in der Rationale von `ClientRequestLoggingFilterTest:904-913`, die Ablehnung von `map`/`doOnCancel`/`doFinally` und die Einsicht "peek operators cannot see when the downstream RETURNED" nur hier (der Guide skizziert noch das verworfene Design - außerhalb des Scopes).
- `…/ObservedBody.kt:17-37` - Why-not `doOnCancel`/`doFinally` und das Thread-Identitätskriterium; die REGEL ist im Guide §4.3 und der README-Tabelle kopiert, die verworfene Alternative nur hier. `:51-59` hat eine Guide-Kopie (📚🟠).
- `…/BoundedBodyCapture.kt:18-26` (WebClient, Lock-und-Freeze) und `./legatium-restclient-logging/…/BoundedBodyCapture.kt:17-22, 55-57, 86` (Single-Writer/Late-Reader und die Invariante "totalBytes written LAST, read FIRST" - eine Schreibreihenfolge-Invariante, die kein `check` ausdrücken kann, an den drei Zeilen, die sie bindet).
- `./legatium-restclient-logging/…/CapturingClientHttpResponse.kt:14-21` (Emit-at-close-Rationale, die nie geschlossene Response bleibt auf dem Gauge "rather than logging a guess") und `:26-28` (Chronik MIT Fehlerbild).
- `./legatium-restclient-logging/…/ClientRequestLoggingInterceptor.kt:320-326` (warum die nebenwirkungsfreien Wiring-Schritte zuerst laufen; einzige Kopie außerhalb des schreibgeschützten Defekt-Analyse-Reports), `:230-234` (verweigernde Engine bei der Übergabe gegen späteren Zugriff des Clients), `:273-276` (peer-kontrollierte `Content-Length` speist nur den Vergleich).
- `…/ExchangeLogEmitter.kt:217-220` (RestClient, Per-Branch-Gründe), `…/ClientLoggingAutoConfiguration.kt:101-103` (RestClient, "Late, not last: room is left below …" - einzige Kopie des Warum von −10), `…/Exchange.kt:82-84` (RestClient, Jettys `HttpFields` als Live-View - warum das Header-OBJEKT gehalten wird).
- `./legatium-webclient-logging/…/ClientRequestLoggingFilter.kt:120-127` (deferContextual um alles, mit dem Fehlerbild "the exception would propagate synchronously past doOnError/doFinally, lose the event and leak the gauge"; gepinnt durch `FilterTest:540, :841` - 📚🟠), `…/CapturingDecorators.kt:66-71` (Zero-copy-Demotion, gepinnt - 📚🟠).
- `./legatium-common/…/FailOpenDiagnostics.kt:35-42` (Exception-nicht-Throwable-Grenze), `Mdc.kt:20-27` (die Parent-ID des Headers IST der lokale Client-Span) und `:34-43` (`ownsTraceKeys`: ungeparste ID wird ENTFERNT, damit sich keine veraltete Bridge-ID anheftet), `ClientLoggingMetrics.kt:109-116` (Same-Type-Gauge-Kollision) und `:363-366` (Weak-both-sides-Rest), `CorrelationIdGenerator.kt:51, 54`, `BodyCapture.kt:57-59`, `RequestTarget.kt:9-18`, `Timeouts.kt:9-18`, `ClientLoggingProperties.kt:154-158`, `HeaderValueMasker.kt:43-47`, `AdapterName.kt:10-14`, `ClientLogFields.kt:37-38`, `HeaderLogProperties.kt:8-11`.
- Tests: `./legatium-webclient-logging/src/test/kotlin/eu/inqudium/legatium/webclient/logging/IntegrationFixture.kt:21-25` und `./legatium-restclient-logging/src/test/kotlin/eu/inqudium/legatium/restclient/logging/TestSupport.kt:131-136` (warum `@BeforeEach`, nicht ein Initializer - Fehlerbild "the first test of a suite would see no events"; heute entstanden, einzige Kopien), `…/Tarpit.kt:8-35` (Linux-Accept-Queue-Semantik).

## 6. Systemic patterns

Zählbasis je Muster; repräsentative Orte.

- **P1 - Twin-identische Kommentarzeilen (fortgeführt aus Runde 1; jetzt 51 statt 40).** Zählbasis: wortgleiche Kommentarzeilen > 40 Zeichen nach Entfernen der Kommentarzeichen in beiden Twin-Modulen (Skript über `*.kt` in `./legatium-{restclient,webclient}-logging/src/main`): Entry Points 16, Emitter 14, `Exchange` 10, Auto-Konfigurationen 6, Captures 5; nach Neutralisierung der Stack-Namen 62 identische plus 43 nahezu identische (difflib ≥ 0,8). Aufteilung: ≈ 20 legitim (öffentliche Konstruktorparameter-, Bean- und `CUSTOMIZER_ORDER`-KDoc, die je Twin allein stehen muss; die Ein-Satz-Verweise, die Runde 1 empfohlen hat - identisch by construction, das ist der gewollte Endzustand), ≈ 15 mit kanonischem Ort in common (Befund 28), ≈ 16 Konzeptabsätze ohne kanonischen Ort (Teardown-Regel, Levels/`classify`, Differenzlisten - Befunde 29, 30, 19). Der Anstieg ist also zu zwei Dritteln Folge der Runde-1-Fixes und der ADR-0009/0011-Commits (identische Konstanten-KDoc), nicht neue Drift. Remedy: Befund 28 in Pass 3; der Konzeptrest erst nach den Ankern aus Pass 2.
- **P2 - Regelwiederholung entlang der Aufrufkette und über Klasse/Member bzw. Feld/Konstante (neue Gestalt von Runde-1-P2 "Beweisführung").** Die Beweisführung ist aus dem Code verschwunden (0 Review-/Prozentzitate; ein Datumsanker). An ihre Stelle tritt: dieselbe Regel an jedem Hop, den ein Fix berührt hat. Zählbasis durch Lesen: Content-Length ×5 (24), Request-Sample ×3 (32), Levels ×2 je Emitter (30), Kundenorder ×2 je Auto-Konfiguration (31), Wiring-Fail-open ×2 und Snapshot-Thread-Regel ×2 (33), Feld/Konstante ×4 (26), Klasse/Property ×4 (27), Breitentest ×2 (39) - ≈ 15 Paare, ≈ 55 Zeilen; sechs der Paare enthalten Text von heute. Ursache: die Fix-Runden erklären an der geänderten Stelle, ohne den kanonischen Ort zu suchen; der Fix von Runde-1-Befund 6 zeigt es exemplarisch (Kopie erzeugt, Quelle nicht gekürzt). Remedy: Pass 3 mit der Review-Frage "wo steht diese Regel schon?"; die Erwartung im Zeilenkonto (≈ −45) ist die größte Kondensationsposition dieser Runde.
- **P3 - Textuelle statt symbolischer Referenzen (fortgeführt), mit neuer Untergruppe Scheinabsicherung.** Zählbasis über `./legatium-*/src/main`: 260 `[…]`-Links in KDoc (alle geprüft, Runde 1: 197), **11 bis 15 `[…]`-Links in `//`-Zeilenkommentaren (Runde 1: 0)**, 10 Backtick-Test-/Dateinamen (mit tragendem Restsatz, akzeptiert; 1 veraltet - Befund 35), 10 Prosa-Verweise "see the …/below/above" (Runde 1: ≈ 10; die Menge ist gleich geblieben, die Stellen nicht - vier der zehn sind von heute oder vom 5. September), 1 Backtick-Produktionssymbol. Befunde 21, 37, 38. Die Disziplin ist vorhanden (260 : 15); die Scheinabsicherung ist das eigentliche Risiko, weil sie geprüft aussieht.
- **P4 - Emphasis-Großschreibung als emergentes Vokabular, kein Präfix-Inventar (fortgeführt, gewachsen).** ≈ 374 Betonungswörter (Runde 1: ≈ 296), 14 `ELK:`, 0 `Invariant:`/`Workaround:`/`Assumption:`. Die Fix-Runden haben neue Invarianten und Workarounds eingeführt, die nicht greppbar sind: die Wiring-Reihenfolge (`Interceptor:320-326`), "origin count LAST" (beide Entry Points), die `@BeforeEach`-Regel der Fixtures, die `takeWhile`-Abhängigkeit (`ObservedBody.kt:23`, Befund 23). Das Präfix-ADR aus Runde-1-Pass 2 (i) existiert nicht. Remedy unverändert: fünf bis sieben Präfixe als kurzes ADR, ohne die Großschreibung abzuschaffen.
- **P5 - Test-Zeremonie per Konvention (fortgeführt, Kostenbild).** 553 nackte gegen 261 beschriftete Stufenlabels (68 %), 339 vollständige Rationale-Blöcke, 61/61 Klassen-KDoc, 76 Twin-identische Test-Kommentarzeilen > 60 Zeichen (Runde 1 zählte 5 doppelte Rationale-Texte auf anderer Basis), 2 ordnungsabhängige Verweise (`ClientRequestLoggingFilterTest:407, 460`). Entschieden (Architektur-Review 2026-09-05, Befund 2); der heute korrigierte Evidence-Generator rendert beide Label-Formen. Kein Befund.
- **P6 - Ungeprüfte Symmetrie-Aussagen zwischen den Twins (neu).** Zählbasis: `grep "RestClient twin|WebClient twin|reactive twin|blocking twin|counterpart|identical to|deliberate"` plus Lesen: 13 im reaktiven Twin (`Filter:27, 30, 46-47, 74`, `Emitter:29, 61, 161, 184, 231, 249`, `AmbientContextRestorer:19-20`, `AutoConfig:87`, `CapturingDecorators:19`, `ObservedBody:56`), 5 im blockierenden (`Interceptor:59-60`, `Exchange:57-58`, `BoundedBodyCapture:37-38`, `CallerMdcSnapshot:22-23`, `AutoConfig:102-103`). Eine ist falsch (17), eine Liste unvollständig (19), eine einseitig (45); 15 verifiziert wahr. `TwinContractTest` pinnt nur die Message-Texte. Ursache: die beiden Commits, die die Twins divergieren ließen (ADR-0011, der Breadcrumb-Fix), haben je eine Seite aktualisiert. Remedy: die Differenzliste des Filters als kanonischen Ort führen (19) und im Review jede Twin-Änderung mit einem Blick in den anderen Twin quittieren - der Code-Style-Report von heute (P2 dort) empfiehlt denselben Schritt für den Code.
- **P7 - Cross-Repo-Aussagen über limesium (neu als Muster; Runde-1-Befund 12).** 7 Stellen (`grep limesium` über `./legatium-*/src/main`): 5 wahr, 1 falsch (18), 1 nicht prüfbar (der `HIGHEST_PRECEDENCE + 10`-Wert ist verifiziert, die Begründung nicht). Das einzige funktionierende Modell ist der Fingerprint: identisches Literal in den Tests beider Repositories. Die Korrelationsgrenze hat keines. Remedy: Befund 18, Pass 4 (Literal-Pin).
- **P8 - ADR-Inhalt am Code paraphrasiert (neu).** ADR-0010 an 6 Stellen des reaktiven Twins (≈ 35 Zeilen), ADR-0011-Spiegel an 4 Stellen des blockierenden; Zählbasis `grep ADR-0010|ADR-0011` plus Lesen. Beide ADRs tragen die Schichtung mit Code-Snippet. Nicht jede Paraphrase ist ein Befund - `withEmissionScopes` braucht die Reihenfolge als Entscheidung -, aber die Herleitung gehört einmal ins ADR (Befund 29).
- **P9 - Kanonischer Ort zählt seine Nutzer auf (neu, Konsistenzbeobachtung).** `Mdc.kt:37-39`, `FailOpenDiagnostics.kt:30-33`, `NoOpScope.kt:5-6`, `BodyCapture.kt:18-19`, `ClientLoggingProperties.kt:93-95`, `DeclaredCharset.kt:10-11` - 6 Stellen durch Lesen, alle heute verifiziert korrekt, keine werkzeuggeprüft; die Methode markiert die Richtung (allgemein → speziell) als Befund, hier nicht als Einzelbefunde geführt, weil jeder Satz ein Warum trägt (welcher Twin und warum anders). Nach ADR-0010/0011 halten die Aufzählungen noch; sie sind die ersten Zeilen, die beim nächsten Twin-Refactoring neu zu prüfen sind.

## 7. Implementation order & guardrails

**Vorab, außerhalb der Pässe:** Befund 17 (zwei Zeilen im reaktiven Twin, davon eine in öffentlicher KDoc) und Befund 18 (ein Satz in `CorrelationHeader.kt`; das ADR-0002-Amendment mit den tatsächlichen Grenzen 200/128 gehört in denselben Commit) als je ein Ein-Commit-Fix, sofort - es sind die beiden Befunde, die einen Leser aktiv irreführen. Ob 128 gegen 200 zu vereinheitlichen ist, entscheidet die Defekt-Spur; der Kommentar darf so lange keine Konsistenz behaupten.

- **Pass 1 - Hygiene (mechanisch, risikofrei):** nur 📚⚪-Fälle: Befund 45 (vierzehn Umbrüche, eine einseitige Klausel), 43 (ein Zahlwort), 37 in den drei Ortsangaben ("(see below)" ×2, "tee tests"), 35 (ein Halbsatz). Keine inhaltliche Änderung in diesen Commits. Weiterhin: (a) den Commit in ein neu anzulegendes `./.git-blame-ignore-revs` aufnehmen (existiert nicht; Runde-1-Pass 1 ist nie gelaufen, weil er fast leer war) und `git config blame.ignoreRevsFile .git-blame-ignore-revs` in CONTRIBUTING dokumentieren; (b) unmittelbar nach einem Merge-Fenster - aktuell erfüllt (PR #8 heute gemerged, keine offenen Branches).
- **Pass 2 - Anker schaffen:** (i) das Präfix-Vokabular als kurzes ADR (aus Runde 1 offen; Vorschlag: `Invariant:`, `Workaround:`, `Rationale:`, `Safety:`, `Compatibility:`, `ELK:` als bestehendes Präfix aufnehmen) - P4 liefert die Kandidaten, die Fix-Runden haben neue geliefert; (ii) die Teardown-Regel der Emitter an einem Ort (Befund 29: `FailOpenDiagnostics.kt`-KDoc oder ein "Fail-open"-Abschnitt in ADR-0010/0011); (iii) die Differenzliste des Filters vervollständigen (19) und den "MDC coverage"-Abschnitt des Interceptors um ADR-0011 ergänzen (20) - beides öffentliche KDoc, beides FÜGT Sätze hinzu; (iv) Konstanten- und Enum-Eintrags-KDoc der öffentlichen API (44). Erst wenn (ii) steht, dürfen die Twin-Kopien der Teardown-Regel fallen.
- **Pass 3 - Redundante Speichen entfernen:** Befunde 21, 22, 24, 25, 26, 27, 28, 29 (nach dem Anker), 30, 31, 32, 33, 34, 36, 37 (Links), 38, 39, 41 gegen die vorhandenen und die in Pass 2 geschaffenen Anker. Erwartetes Zeilenkonto: **minus 80 bis 100 Produktionskommentarzeilen**; die Dekompositionsbilanz ist null (keine Kandidaten). Reihenfolge: Pilotmodul `legatium-restclient-logging` zuerst (Befund 24 als Übungsfall "eine Regel, ein Ort"), dann `legatium-common`, zuletzt der reaktive Twin.
- **Pass 4 - Träger schaffen (Refactoring):** Befund 23 (Test-Pin für `state=partial` beim Void-Skip), 40 (Reflection-Test für den `toString`-Spiegel), 42 (`require(maxBytes >= 0)` in beiden Captures), 18 (Literal-Pin der Korrelationsgrenze in beiden Repositories), plus die Code-Duplikate `captureFor`/`newCaptures` und `BoundedBodyCapture.loggedValue` (Überlappung aus Befund 28 und dem Code-Style-Report). Codeänderungen mit Testfolge, eigene Branches, nie in einem Kommentar-PR; am günstigsten gekoppelt an die nächste fachliche Änderung am Emitter oder an der Body-Capture (die Churn-Spitzen).
- **Pass 5 - Nachhaltigkeit:** siehe Guardrails.

**PR-Schnitt:** ein PR pro Kategorie - "Verschiebe `[Symbol]`-Links aus Zeilenkommentaren in KDoc (alle Module)", "Eine Regel, ein Ort: Content-Length, Levels, Kundenorder, Meter-Zweck", "Differenzlisten der Twins vervollständigen", "Twin-Feld-KDocs auf Common-Verweise" - nicht pro Modul.

**Guardrails (vorgeschlagen, nicht umgesetzt):**
- **Umgesetzt seit Runde 1:** Dokka im PR-Check (`ci.yml:50`). Die Lücke, die bleibt, ist der Zeilenkommentar: ein `grep`-Schritt im CI-Job `build` auf `^\s*//.*\[[A-Za-z]\w*(\.\w+)?\]` über `*/src/main/**/*.kt` mit Abbruch bei > 0 macht die Scheinabsicherung (P3) unmöglich - billiger als jede Review-Regel.
- Präfix-Vokabular als ADR (Pass 2), danach die Review-Frage statt einer Quote: "Kann dieser Kommentar falsch werden, ohne dass etwas rot wird? Wenn ja - kann ein Name, ein Typ, ein `check`, ein Test oder ein `[Symbol]`-Link in KDoc ihn tragen?" Ergänzt um die zwei Fragen dieser Runde: "**Wo steht diese Regel schon?**" (P2) und "**Was sagt der andere Twin über dieselbe Stelle?**" (P6) - die zweite als Pflichtzeile in der PR-Vorlage für jede Änderung unter `legatium-*-logging/src/main`.
- Cross-Repo-Kontrakte (Fingerprint, Korrelationsgrenze, Präfixe) gehören als identisches Literal in die Tests beider Repositories; die Kommentare nennen den Pin, nicht die Behauptung - der Fingerprint ist das Vorbild, die Korrelationsgrenze der Gegenbeweis (P7).
- Harte Regeln nur für Eindeutiges (eingehalten, festschreiben): kein auskommentierter Code; ein TODO braucht Ticket oder Phasen-Tag; öffentliche Deklarationen tragen KDoc auf Methoden-, Parameter-, Konstanten- und Enum-Eintrags-Ebene.

**Ausdrücklich nicht vorgeschlagen:** eine Kommentarquote oder ein Zähler gelöschter Kommentare. Bei 41 % Kommentaranteil wäre der billigste Weg zu einer Quote das Löschen der Warum-Kommentare - genau des Bestands, der oben als Schutzstellen markiert ist. Ebenso nicht vorgeschlagen: die Twin-Duplikate auf null zu bringen - die Ein-Satz-Verweise und die öffentliche Parameter-KDoc SOLLEN in beiden Twins stehen (ADR-0003).

**Abbruchkriterien (Zustandsgrößen mit erreichbarer Null):** auskommentierte Blöcke → 0 (erreicht); unauflösbare Dokumentationsreferenzen → 0 (erreicht, jetzt im PR-Check gehalten); `@param` ohne Parameter → 0 (erreicht); TODOs ohne Tag und Ticket → 0 (erreicht); Review-/Prozent-Referenzen in Produktionskommentaren → 0 (erreicht seit Runde 1); **`[Symbol]`-Links in `//`-Zeilenkommentaren → 0 (aktuell 11-15)**; Backtick-Produktionssymbole statt `[Symbol]` → 0 (aktuell 2: `CorrelationIdGenerator.kt:8`, `Exchange.kt:33` RestClient); falsche oder unvollständige Symmetrie-Aussagen zwischen den Twins → 0 (aktuell 3 von 18); falsche Cross-Repo-Aussagen → 0 (aktuell 1 von 7); Regeln mit mehr als einer vollständigen Herleitung im Modul → 0 (aktuell ≈ 15 Paare); öffentliche Deklarationen ohne eigene KDoc → 0 (aktuell 1 Konstante, 3 Enum-Einträge, 3 `MdcKeys`-Konstanten); Kommentare der definierten Kategorien ohne Präfix → 0 (erst nach Pass 2 messbar); Testklassen ohne Klassen-KDoc → 0 (erreicht); unvollständige Rationale-Blöcke → 0 (erreicht, vom Evidence-Generator gehalten).

---

*Kein Code wurde durch diese Analyse verändert. Die 16 Befunde des Erstberichts bleiben dort unverändert; ihr Status steht in Abschnitt 5.0 dieses Berichts. Jede "Carrier strategy" oben beschreibt die Strategie, anzuwenden - oder abzulehnen - Datei für Datei in einer separaten Sitzung.*
